# 주가 수집 종목 확대 - 5가지 해결 방법 비교

## 🎯 문제 상황

```
현재:  958개 (KOSPI만)
목표: 2,759개 (KOSPI + KOSDAQ)
손실: 1,801개 (약 65% 누락!)
```

### 왜 이런 일이?

```python
# 문제 코드
tickers = pykrx_stock.get_market_ticker_list(date="20251028")
# 결과: KOSPI 958개만 반환 (기본값!)

# 올바른 방법
kosdaq_tickers = pykrx_stock.get_market_ticker_list(
    market="KOSDAQ",  # 명시적 지정 필수!
    date="20251028"
)
# 결과: KOSDAQ 1,801개 반환
```

---

## 📊 5가지 방법 상세 비교

### **방법 1: 마켓별 별도 수집** ⭐⭐⭐⭐⭐ 권장

#### 개념도
```
┌─────────────────────┐
│  daily_collection   │
│      _dag.py        │
└──────────┬──────────┘
           │
      ┌────┴────┐
      ▼         ▼
  KOSPI    KOSDAQ
  958개    1,801개
      │         │
      └────┬────┘
           ▼
    총 2,759개
```

#### 코드 예제
```python
def fetch_stock_prices_api(target_date):
    all_prices = []

    # KOSPI + KOSDAQ 모두 수집
    for market in ["KOSPI", "KOSDAQ"]:
        tickers = pykrx_stock.get_market_ticker_list(
            market=market,
            date=date_str
        )

        for ticker in tickers:
            try:
                df = pykrx_stock.get_market_ohlcv(date_str, date_str, ticker)
                if not df.empty:
                    df['ticker'] = ticker
                    all_prices.append(df)
            except:
                pass

    return pd.concat(all_prices, ignore_index=True)
```

#### 장점
- ✅ 구현 매우 간단 (10줄)
- ✅ 지금 당장 적용 가능 (1시간)
- ✅ 2,759개 모두 수집 보장
- ✅ 코드 가독성 좋음

#### 단점
- ❌ 수집 시간 2배 증가 (5분 → 10분)
- ❌ 마켓 추가 시 코드 수정 필요

#### 구현 시간: **30분**
#### 추천: **BEST CHOICE** 🏆

---

### **방법 2: DB 종목 마스터 테이블** ⭐⭐⭐⭐⭐

#### 개념도
```
┌──────────────┐
│ stocks 테이블 │  (2,759개 미리 저장)
├──────────────┤
│ ticker (PK)  │
│ is_active    │
│ market       │
│ listed_date  │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│ fetch_prices()  │
│ 테이블에서 읽기  │
└─────────────────┘
```

#### 코드 예제
```python
def fetch_stock_prices_api(target_date):
    # DB에서 활성 종목만 읽기
    cursor.execute(
        "SELECT ticker FROM stocks WHERE is_active = true"
    )
    tickers = [row[0] for row in cursor.fetchall()]

    all_prices = []
    for ticker in tickers:
        try:
            df = pykrx_stock.get_market_ohlcv(date_str, date_str, ticker)
            if not df.empty:
                df['ticker'] = ticker
                all_prices.append(df)
        except:
            pass

    return pd.concat(all_prices, ignore_index=True)
```

#### 장점
- ✅ 가장 정확 (상장폐지 추적 가능)
- ✅ 신규 상장 자동 발견
- ✅ 수집 실패율 최소화
- ✅ 운영 기록 유지

#### 단점
- ❌ 초기 구축 시간 (2시간)
- ❌ stocks 테이블 동기화 필요
- ❌ 상장폐지 종목 관리 필요

#### 구현 시간: **2시간**
#### 추천: **장기 목표** 🎯

---

### **방법 3: 동적 종목 발견** ⭐⭐⭐

#### 개념도
```
실행 시점:
─ KOSPI 조회
─ KOSDAQ 조회
─ KONEX 조회
─ 기타 마켓 조회
    │
    ▼
  합집합 (자동 중복 제거)
```

#### 코드 예제
```python
def fetch_all_available_tickers(date_str):
    all_tickers = set()

    # 모든 마켓 조회
    for market in ["KOSPI", "KOSDAQ", "KONEX"]:
        try:
            tickers = pykrx_stock.get_market_ticker_list(
                market=market,
                date=date_str
            )
            all_tickers.update(tickers)
            logger.info(f"{market}: {len(tickers)} tickers")
        except Exception as e:
            logger.warning(f"{market} failed: {e}")

    logger.info(f"Total: {len(all_tickers)} tickers")
    return list(all_tickers)
```

#### 장점
- ✅ KONEX 종목도 포함 (신흥기업)
- ✅ 새로운 마켓 추가 용이
- ✅ 자동으로 중복 제거

#### 단점
- ❌ API 호출 3배 증가
- ❌ 실패 처리 복잡
- ❌ KONEX 종목은 거래량 매우 적음

#### 구현 시간: **20분**
#### 추천: **한정적** (특수 목적)

---

### **방법 4: 배치 + 실패 재시도** ⭐⭐⭐⭐

#### 개념도
```
┌─────────────────┐
│ 모든 종목 2,759개│
└────────┬────────┘
         │
    ┌────▼────────────────┐
    │  배치 처리 (100개씩) │
    └────┬─────────────────┘
         │
    ┌────▼──────────┐
    │  성공 ──────► DB
    │  │
    │  실패 ──────► 재시도 리스트
    └────┬──────────┘
         │
    ┌────▼──────────┐
    │  재시도 (3회) │
    └────┬──────────┘
         │
    ┌────▼──────────┐
    │  최종 실패    │
    │  건너뜀      │
    └──────────────┘
```

#### 코드 예제
```python
def fetch_stock_prices_with_retry(target_date):
    # 1단계: 모든 종목 가져오기
    all_tickers = []
    for market in ["KOSPI", "KOSDAQ"]:
        tickers = pykrx_stock.get_market_ticker_list(market=market)
        all_tickers.extend(tickers)

    # 2단계: 배치 처리
    batch_size = 100
    all_prices = []
    failed_tickers = []

    for i in range(0, len(all_tickers), batch_size):
        batch = all_tickers[i:i+batch_size]
        for ticker in batch:
            try:
                df = pykrx_stock.get_market_ohlcv(date_str, date_str, ticker)
                if not df.empty:
                    df['ticker'] = ticker
                    all_prices.append(df)
            except Exception as e:
                failed_tickers.append((ticker, str(e)))

    # 3단계: 실패한 종목 재시도
    logger.info(f"Failed on first attempt: {len(failed_tickers)}")
    for ticker, _ in failed_tickers:
        try:
            df = pykrx_stock.get_market_ohlcv(date_str, date_str, ticker)
            if not df.empty:
                df['ticker'] = ticker
                all_prices.append(df)
        except:
            pass  # 최종 실패는 무시

    return pd.concat(all_prices, ignore_index=True)
```

#### 장점
- ✅ 높은 수집 성공률
- ✅ 재시도 로직으로 신뢰성 향상
- ✅ 성능 최적화 (배치 처리)
- ✅ 실패 원인 기록

#### 단점
- ❌ 코드 복잡도 증가 (30줄)
- ❌ 구현 시간 더 필요 (1.5시간)
- ❌ 메모리 사용량 증가

#### 구현 시간: **1.5시간**
#### 추천: **중규모 팀** 👥

---

### **방법 5: 설정 파일 기반** ⭐⭐⭐⭐⭐

#### 개념도
```
┌──────────────┐
│  config.json │  (수집 정책 정의)
└──────┬───────┘
       │
   ┌───┴─────────────────┐
   │                     │
▼  ▼                     ▼
마켓  제외 종목      추가 종목
목록  명단            명단
   │                     │
   └─────────┬───────────┘
             │
         ┌───▼────────┐
         │ 동적 결정   │
         │ (운영 중)   │
         └─────┬──────┘
               │
               ▼
         수집 종목 리스트
```

#### 코드 예제

**config.json:**
```json
{
  "markets": ["KOSPI", "KOSDAQ"],
  "collection_settings": {
    "batch_size": 100,
    "retry_count": 3,
    "timeout_seconds": 30,
    "skip_suspended": true
  },
  "excluded_tickers": [
    "005930",  # 삼성전자 제외 예시
    "000660"   # SK하이닉스 제외 예시
  ],
  "included_tickers": [
    "999999"   # 특수 종목 추가 예시
  ]
}
```

**Python 코드:**
```python
import json

def fetch_stock_prices_api(target_date):
    # 설정 로드
    with open('/app/config.json', 'r') as f:
        config = json.load(f)

    settings = config['collection_settings']
    all_tickers = set()

    # 마켓별 수집
    for market in config['markets']:
        try:
            tickers = pykrx_stock.get_market_ticker_list(market=market)
            all_tickers.update(tickers)
        except:
            pass

    # 제외 종목 제거
    all_tickers -= set(config['excluded_tickers'])

    # 추가 종목 포함
    all_tickers.update(config['included_tickers'])

    # 수집 로직
    all_prices = []
    for ticker in all_tickers:
        try:
            df = pykrx_stock.get_market_ohlcv(date_str, date_str, ticker)
            if not df.empty:
                df['ticker'] = ticker
                all_prices.append(df)
        except:
            pass

    return pd.concat(all_prices, ignore_index=True)
```

#### 장점
- ✅ 운영 중 수집 정책 변경 가능
- ✅ 코드 수정 없이 설정만 변경
- ✅ 신규/폐지 종목 관리 용이
- ✅ 팀 전체가 이해하기 쉬움

#### 단점
- ❌ 초기 설정 시간 필요
- ❌ config.json 동기화 필요
- ❌ 설정 버전 관리 필요

#### 구현 시간: **1시간**
#### 추천: **엔터프라이즈** 🏢

---

## 📋 최종 비교 요약표

| 항목 | 방법1 | 방법2 | 방법3 | 방법4 | 방법5 |
|------|-------|-------|-------|-------|-------|
| **구현 난도** | ⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **정확도** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **유연성** | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **성능** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **유지보수** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **구현 시간** | 30분 | 2시간 | 20분 | 1.5시간 | 1시간 |
| **안정성** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🎯 상황별 추천

### 상황 1: "지금 당장 2,700개 수집하고 싶어요"
→ **방법 1 (마켓별 별도 수집)**
- 30분 안에 구현 가능
- 2,759개 모두 수집
- 코드 간단

### 상황 2: "한 번 제대로 구축해서 오래 운영할 거예요"
→ **방법 5 (설정 파일 기반)** + **방법 2 (DB 마스터)**
- Phase 1: 방법 5로 빠르게 구축 (1시간)
- Phase 2: 방법 2로 장기 운영 (2시간)

### 상황 3: "가장 안정적이고 정확하게"
→ **방법 4 (배치 + 실패 재시도)**
- 높은 수집 성공률
- 실패 추적 가능
- 신뢰성 최고

### 상황 4: "최대한 단순하게"
→ **방법 1 (마켓별 별도 수집)**
- 가장 간단
- 버그 가능성 최소

### 상황 5: "나중에 확장할 수 있게"
→ **방법 5 (설정 파일)**
- KONEX 추가? → config.json 수정
- 특정 종목 제외? → config.json 수정
- 수집 재시도? → config.json 수정

---

## 🚀 추천 로드맵

```
Day 1 (지금):
└─ 방법 1 구현 (30분)
   └─ KOSDAQ 추가 (2,759개 수집)

Day 2:
└─ 방법 5 리팩토링 (1시간)
   └─ config.json으로 정책 관리

Day 3-5:
└─ 방법 2 구축 (2시간)
   └─ stocks 테이블 동기화 자동화
   └─ 상장폐지/신규 상장 추적
```

---

## ⚠️ 주의사항

1. **API 호출 제한**
   - pykrx는 시간당 제한 있을 수 있음
   - 테스트 후 확인 필수

2. **수집 시간**
   - 현재: ~5분 (958개)
   - 방법 1: ~10-15분 (2,759개)
   - 방법 4: ~5-8분 (배치 처리로 빠름)

3. **메모리 사용**
   - 2,759개 × 8개 OHLCV = ~22,000행
   - 메모리 20MB 정도 필요

4. **상장폐지 종목**
   - 실제로는 2,200개 정도만 거래 가능
   - 상장폐지 종목은 자동 제외됨

---

## 📞 선택 가이드

**어떤 방법을 선택해야 할까요?**

```
┌─ 급할 때? (30분) → 방법 1
│
├─ 정확하게? → 방법 2 또는 4
│
├─ 유연하게? → 방법 5
│
└─ 안정적으로? → 방법 4
```

**아직 결정 못했나요?**
→ **방법 1부터 시작하세요!** (30분)
→ 나중에 필요하면 방법 5로 리팩토링 가능
