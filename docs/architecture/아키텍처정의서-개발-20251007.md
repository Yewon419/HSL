# 주식 트레이딩 시스템 아키텍처 정의서

## 문서 정보
- **프로젝트명**: Stock Trading System (HSL - Happy Stock Life)
- **버전**: 1.0
- **작성일**: 2025-10-06
- **시스템 유형**: 마이크로서비스 기반 주식 자동매매 플랫폼

---

## 목차
1. [시스템 개요](#1-시스템-개요)
2. [전체 시스템 아키텍처](#2-전체-시스템-아키텍처)
3. [백엔드 아키텍처](#3-백엔드-아키텍처)
4. [데이터베이스 아키텍처](#4-데이터베이스-아키텍처)
5. [UI 아키텍처](#5-ui-아키텍처)
6. [통신 프로토콜](#6-통신-프로토콜)
7. [보안 아키텍처](#7-보안-아키텍처)
8. [배포 아키텍처](#8-배포-아키텍처)

---

## 1. 시스템 개요

### 1.1 목적
한국 주식시장(KOSPI/KOSDAQ) 데이터를 실시간으로 수집·분석하여 기술적 지표 기반 자동매매 시그널을 제공하고, 포트폴리오 관리 및 백테스팅 기능을 통합한 통합 트레이딩 플랫폼

### 1.2 핵심 기능
- 📊 **주가 데이터 수집**: 한국/미국 주식시장 일별 시세 데이터 자동 수집
- 📈 **기술적 지표 계산**: RSI, MACD, 스토캐스틱, 볼린저밴드, 일목균형표
- 🎯 **매매 시그널 감지**: RSI+MA 상승전략, 과매도/과매수 감지
- 💼 **포트폴리오 관리**: 매수/매도 관리, 수익률 추적
- 🔍 **종목 스크리닝**: 다중 조건 필터링, 전략 템플릿
- 📉 **백테스팅**: 과거 데이터 기반 전략 성과 분석
- 📲 **멀티채널 알림**: 이메일, 카카오톡, Slack 알림

### 1.3 기술 스택 요약

| 계층 | 기술 |
|------|------|
| **Frontend** | HTML5, Bootstrap 5, Chart.js, Vanilla JavaScript |
| **Backend** | Python 3.10+, FastAPI, SQLAlchemy, Pydantic |
| **Database** | PostgreSQL 14 + TimescaleDB, Redis, InfluxDB |
| **Monitoring** | Grafana, InfluxDB |
| **Orchestration** | Apache Airflow |
| **Task Queue** | Celery + Redis |
| **Container** | Docker, Docker Compose |
| **Authentication** | JWT (JSON Web Token) |

---

## 2. 전체 시스템 아키텍처

### 2.1 시스템 레이어 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Presentation Layer                            │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  Web UI (HTML/CSS/JavaScript)                                    │  │
│  │  - index.html (메인 대시보드)                                   │  │
│  │  - rsi_ma_strategy.html (매매 시그널)                          │  │
│  │  - screening.html (종목 스크리닝)                               │  │
│  │  - stock_grid.html (종목 관리)                                  │  │
│  │  - Grafana Dashboards (실시간 차트)                            │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ HTTP/REST API (Port 8000)
┌────────────────────────────┴────────────────────────────────────────────┐
│                          Application Layer                              │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │               FastAPI Backend (7 Microservices)                  │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐    │  │
│  │  │ User Service   │  │ Stock Service  │  │ Indicators     │    │  │
│  │  │ /api/v1/users  │  │ /api/stocks    │  │ /api/v1/       │    │  │
│  │  │                │  │                │  │ indicators     │    │  │
│  │  └────────────────┘  └────────────────┘  └────────────────┘    │  │
│  │                                                                  │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐    │  │
│  │  │ Trading        │  │ Screening      │  │ Simulation     │    │  │
│  │  │ /api/trading   │  │ /api/v1/       │  │ /api/v1/       │    │  │
│  │  │                │  │ screening      │  │ simulations    │    │  │
│  │  └────────────────┘  └────────────────┘  └────────────────┘    │  │
│  │                                                                  │  │
│  │  ┌────────────────┐                                             │  │
│  │  │ AI Service     │  Celery Workers (Background Tasks)          │  │
│  │  │ /api/v1/ai     │  - 데이터 수집                              │  │
│  │  │                │  - 지표 계산                                │  │
│  │  └────────────────┘  - 알림 전송                                │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└────┬────────────┬────────────┬────────────┬───────────┬───────────────┘
     │            │            │            │           │
┌────┴────┐  ┌───┴────┐  ┌────┴─────┐  ┌──┴──────┐  ┌┴──────────┐
│PostgreSQL│  │ Redis  │  │ InfluxDB │  │ Grafana │  │  Airflow  │
│TimeScale │  │ Cache  │  │ Metrics  │  │ Dashbd  │  │ Scheduler │
│  :5435   │  │ :6380  │  │  :8086   │  │  :3000  │  │   :8080   │
└──────────┘  └────────┘  └──────────┘  └─────────┘  └───────────┘
     │            │            │
┌────┴────────────┴────────────┴───────────────────────────────────┐
│                    Data Persistence Layer                        │
│  - stock_prices (시계열 OHLCV 데이터)                           │
│  - technical_indicators (기술적 지표)                           │
│  - trading_signals (매매 시그널)                                │
│  - portfolios (사용자 포트폴리오)                               │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 데이터 흐름도 (Data Flow)

```
┌──────────────┐
│ 외부 데이터  │
│ - pykrx API  │
│ - yfinance   │
│ - KRX        │
└──────┬───────┘
       │
       ↓ (1) Airflow DAG Scheduling
┌──────────────────────────────────────────┐
│   데이터 수집 (Stock Service)            │
│   - fetch_korea_stocks.py                │
│   - korea_stock_fetcher.py               │
└──────┬───────────────────────────────────┘
       │
       ↓ (2) Raw Data Insert
┌──────────────────────────────────────────┐
│   PostgreSQL (stock_prices)              │
│   - ticker, date, OHLCV                  │
└──────┬───────────────────────────────────┘
       │
       ↓ (3) Celery Task Trigger
┌──────────────────────────────────────────┐
│   지표 계산 (Indicators Service)         │
│   - calculate_all_indicators.py          │
│   - TechnicalIndicators class            │
└──────┬───────────────────────────────────┘
       │
       ↓ (4) Indicators Insert
┌──────────────────────────────────────────┐
│   PostgreSQL (technical_indicators)      │
│   - RSI, MACD, Stochastic, Bollinger    │
└──────┬───────────────────────────────────┘
       │
       ↓ (5) Signal Detection
┌──────────────────────────────────────────┐
│   시그널 감지 (Trading Service)          │
│   - SignalDetector.detect_buy_signals()  │
│   - 조건: RSI<30 AND MA20>MA50           │
└──────┬───────────────────────────────────┘
       │
       ↓ (6) Signal Insert
┌──────────────────────────────────────────┐
│   PostgreSQL (trading_signals)           │
│   - BUY/SELL/STOP_LOSS                   │
└──────┬───────────────────────────────────┘
       │
       ↓ (7) Notification Trigger
┌──────────────────────────────────────────┐
│   알림 전송 (NotificationService)        │
│   - Email, Kakao, Slack                  │
└──────────────────────────────────────────┘
       │
       ↓ (8) Metrics Logging
┌──────────────────────────────────────────┐
│   InfluxDB (시계열 메트릭)               │
│   - API 응답시간, 시그널 빈도            │
└──────┬───────────────────────────────────┘
       │
       ↓ (9) Visualization
┌──────────────────────────────────────────┐
│   Grafana Dashboards                     │
│   - 주가 차트, 지표 오버레이             │
└──────────────────────────────────────────┘
```

---

## 3. 백엔드 아키텍처

### 3.1 마이크로서비스 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                   FastAPI Application (main.py)                 │
│                      Port 8000 (HTTP/REST)                      │
└────┬────────────────────────────────────────────────────────────┘
     │
     ├─ CORS Middleware (허용: localhost:3000, localhost:8080)
     ├─ JWT Authentication Middleware
     ├─ Logging Middleware
     │
     ├─────────────────────────────────────────────────────────────┐
     │                   7 Microservices Routers                   │
     ├─────────────────────────────────────────────────────────────┤
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  1. User Service (/api/v1/users)                     │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/user_service/router.py              │  │
     │  │  [인증] JWT Token (auth.py)                         │  │
     │  │                                                      │  │
     │  │  주요 엔드포인트:                                    │  │
     │  │  • POST /register       - 사용자 등록               │  │
     │  │  • POST /token          - 로그인 (JWT 발급)         │  │
     │  │  • GET  /me             - 현재 사용자 정보          │  │
     │  │  • GET  /dashboard      - 포트폴리오 대시보드       │  │
     │  │  • POST /portfolio      - 종목 매수                 │  │
     │  │  • POST /portfolio/sell - 종목 매도                 │  │
     │  │  • GET  /alerts         - 사용자 알림 조회          │  │
     │  │                                                      │  │
     │  │  핵심 기능:                                          │  │
     │  │  - bcrypt 비밀번호 해싱                             │  │
     │  │  - JWT 토큰 생성/검증 (24시간 만료)                │  │
     │  │  - 포트폴리오 수익률 계산                           │  │
     │  │  - 최고/최저 수익 종목 분석                         │  │
     │  │                                                      │  │
     │  │  데이터 모델:                                        │  │
     │  │  - User (users 테이블)                              │  │
     │  │  - Portfolio (portfolios 테이블)                    │  │
     │  │  - UserAlert (user_alerts 테이블)                   │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  2. Stock Service (/api/stocks)                      │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/stock_service/data_fetcher.py       │  │
     │  │        backend/stock_service/indicators.py          │  │
     │  │        backend/stock_service/korea_stock_fetcher.py │  │
     │  │                                                      │  │
     │  │  핵심 클래스:                                        │  │
     │  │  - StockDataFetcher: 주가 데이터 수집               │  │
     │  │    • fetch_stock_data() - yfinance API 호출         │  │
     │  │    • save_stock_prices() - DB 저장                  │  │
     │  │    • save_technical_indicators() - 지표 저장        │  │
     │  │                                                      │  │
     │  │  - MarketScreener: 시장 스크리닝                    │  │
     │  │    • find_oversold_stocks() - RSI<30 종목 검색      │  │
     │  │    • find_overbought_stocks() - RSI>70 종목         │  │
     │  │    • find_macd_crossovers() - MACD 크로스오버       │  │
     │  │                                                      │  │
     │  │  - TechnicalIndicators: 기술적 지표 계산            │  │
     │  │    • calculate_all_indicators(df)                   │  │
     │  │    • calculate_rsi() - 상대강도지수                 │  │
     │  │    • calculate_macd() - MACD                        │  │
     │  │    • calculate_stochastic() - 스토캐스틱            │  │
     │  │    • calculate_bollinger_bands() - 볼린저밴드       │  │
     │  │                                                      │  │
     │  │  데이터 소스:                                        │  │
     │  │  - 한국 주식: pykrx (한국거래소 API)                │  │
     │  │  - 미국 주식: yfinance (Yahoo Finance API)          │  │
     │  │                                                      │  │
     │  │  데이터 모델:                                        │  │
     │  │  - Stock (stocks 테이블)                            │  │
     │  │  - StockPrice (stock_prices 테이블)                 │  │
     │  │  - StockFundamental (stock_fundamentals 테이블)     │  │
     │  │  - InvestorTrade (investor_trades 테이블)           │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  3. Indicators Service (/api/v1/indicators)          │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/indicators_service/                 │  │
     │  │                                                      │  │
     │  │  계산 지표:                                          │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 추세 지표                                   │    │  │
     │  │  │ - MA (20, 50, 200일)                        │    │  │
     │  │  │ - MACD (12, 26, 9)                          │    │  │
     │  │  │ - Ichimoku (일목균형표)                     │    │  │
     │  │  │   • 전환선 (Tenkan)                         │    │  │
     │  │  │   • 기준선 (Kijun)                          │    │  │
     │  │  │   • 선행스팬 A/B (Senkou A/B)               │    │  │
     │  │  │   • 후행스팬 (Chikou)                       │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 모멘텀 지표                                 │    │  │
     │  │  │ - RSI (14일, 0-100)                         │    │  │
     │  │  │ - Stochastic (K%: 14, D%: 3)                │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 변동성 지표                                 │    │  │
     │  │  │ - Bollinger Bands (20일, 2 표준편차)        │    │  │
     │  │  │   • Upper Band                              │    │  │
     │  │  │   • Middle Band (20MA)                      │    │  │
     │  │  │   • Lower Band                              │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  데이터 모델:                                        │  │
     │  │  - TechnicalIndicator (technical_indicators 테이블) │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  4. Trading Service (/api/trading)                   │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/trading_service/router.py           │  │
     │  │        backend/trading_service/signal_detector.py   │  │
     │  │        backend/trading_service/position_manager.py  │  │
     │  │        backend/trading_service/notification_service.│  │
     │  │                                                      │  │
     │  │  ★ 핵심 매매 전략 (RSI+MA 상승전략)                 │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 매수 시그널 조건:                           │    │  │
     │  │  │  1. RSI < 30 (과매도 구간)                  │    │  │
     │  │  │  2. MA20 > MA50 (상승 추세)                 │    │  │
     │  │  │  → 과매도 구간에서 상승추세 포착            │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 매도 시그널 조건:                           │    │  │
     │  │  │  1. RSI > 70 (과매수 구간)                  │    │  │
     │  │  │  2. 보유 포지션 존재                        │    │  │
     │  │  │  → 과열 구간 이익실현                       │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 손절 시그널 조건:                           │    │  │
     │  │  │  1. 현재가 <= 손절가 (매수가 × 0.95)        │    │  │
     │  │  │  2. 보유 포지션 존재                        │    │  │
     │  │  │  → 5% 손실 시 자동 손절                     │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  주요 엔드포인트:                                    │  │
     │  │  • GET  /signals/buy         - 매수 시그널 조회     │  │
     │  │  • GET  /signals/sell        - 매도 시그널 조회     │  │
     │  │  • GET  /signals/stop-loss   - 손절 시그널 조회     │  │
     │  │  • GET  /signals/all         - 전체 시그널 조회     │  │
     │  │  • POST /positions/buy       - 매수 실행            │  │
     │  │  • POST /positions/sell      - 매도 실행            │  │
     │  │  • GET  /positions/open      - 보유 포지션 조회     │  │
     │  │  • GET  /performance/summary - 수익률 요약          │  │
     │  │  • POST /notifications/send-signals - 알림 전송     │  │
     │  │                                                      │  │
     │  │  핵심 클래스:                                        │  │
     │  │  - SignalDetector: SQL 기반 시그널 감지             │  │
     │  │  - PositionManager: 포지션 CRUD 및 수익률 계산      │  │
     │  │  - NotificationService: 멀티채널 알림               │  │
     │  │    • send_email() - SMTP Gmail                      │  │
     │  │    • send_kakao() - Kakao REST API                  │  │
     │  │    • send_slack() - Slack Webhook                   │  │
     │  │                                                      │  │
     │  │  데이터 모델:                                        │  │
     │  │  - TradingSignal (trading_signals 테이블)           │  │
     │  │  - TradingPosition (trading_positions 테이블)       │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  5. Screening Service (/api/v1/screening)            │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/screening_service/router.py         │  │
     │  │        backend/screening_service/screener.py        │  │
     │  │                                                      │  │
     │  │  주요 기능:                                          │  │
     │  │  - 다중 조건 스크리닝 (여러 지표 AND/OR 조합)       │  │
     │  │  - 전략 템플릿 저장/불러오기 (JSON 파일)            │  │
     │  │  - 백테스팅 엔진 (과거 데이터 기반 성과 분석)       │  │
     │  │                                                      │  │
     │  │  ★ 기본 제공 전략:                                  │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 1. 모멘텀 돌파 전략                         │    │  │
     │  │  │    - RSI 골든크로스 (5일)                   │    │  │
     │  │  │    - MACD 골든크로스 (3일)                  │    │  │
     │  │  │    - ADX > 25 (강한 추세)                   │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 2. 과매도 반등 전략                         │    │  │
     │  │  │    - RSI < 25                               │    │  │
     │  │  │    - MFI < 20 (자금흐름지수)                │    │  │
     │  │  │    - CCI < -100 (상품채널지수)              │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  ┌─────────────────────────────────────────────┐    │  │
     │  │  │ 3. 안정적 추세 전략                         │    │  │
     │  │  │    - RSI 40~60 (중립)                       │    │  │
     │  │  │    - ADX > 20 (추세 시작)                   │    │  │
     │  │  │    - MACD 골든크로스 (7일)                  │    │  │
     │  │  └─────────────────────────────────────────────┘    │  │
     │  │                                                      │  │
     │  │  주요 엔드포인트:                                    │  │
     │  │  • GET  /indicators             - 지표 목록         │  │
     │  │  • POST /screen                 - 스크리닝 실행     │  │
     │  │  • POST /backtest               - 백테스팅 실행     │  │
     │  │  • GET  /strategies             - 전략 목록         │  │
     │  │  • POST /strategies             - 전략 저장         │  │
     │  │  • POST /strategies/{name}/execute - 전략 실행     │  │
     │  │                                                      │  │
     │  │  핵심 클래스:                                        │  │
     │  │  - StockScreener: 다중 조건 필터링                  │  │
     │  │  - BacktestEngine: 백테스팅 시뮬레이션              │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  6. Simulation Service (/api/v1/simulations)         │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/simulation_service/router.py        │  │
     │  │                                                      │  │
     │  │  주요 기능:                                          │  │
     │  │  - 과거 데이터 기반 매매 전략 시뮬레이션            │  │
     │  │  - 리스크/수익률 계산                               │  │
     │  │  - 최대 낙폭(MDD) 계산                              │  │
     │  │                                                      │  │
     │  │  데이터 모델:                                        │  │
     │  │  - Simulation (simulations 테이블)                  │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     │  ┌──────────────────────────────────────────────────────┐  │
     │  │  7. AI Service (/api/v1/ai)                          │  │
     │  │  ─────────────────────────────────────────────────── │  │
     │  │  [파일] backend/ai_service/router.py                │  │
     │  │                                                      │  │
     │  │  주요 기능:                                          │  │
     │  │  - 머신러닝 기반 주가 예측                          │  │
     │  │  - 패턴 인식 (Head & Shoulders, Cup & Handle 등)   │  │
     │  │  - 감성 분석 (뉴스/소셜미디어)                      │  │
     │  └──────────────────────────────────────────────────────┘  │
     │                                                             │
     └─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   Background Task Workers                       │
├─────────────────────────────────────────────────────────────────┤
│  Celery Workers (비동기 작업 처리)                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  작업 큐 (Redis Broker)                                   │ │
│  │  - 데이터 수집 작업 (주기: 매일 장 마감 후)              │ │
│  │  - 기술적 지표 계산 (대량 데이터 처리)                   │ │
│  │  - 매매 시그널 감지 (실시간 모니터링)                    │ │
│  │  - 알림 전송 (이메일/카카오/Slack)                       │ │
│  │  - 백테스팅 실행 (CPU 집약적 작업)                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Celery Beat (스케줄러)                                        │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  주기적 작업 스케줄                                       │ │
│  │  - 09:00: 장 시작 전 시그널 확인                         │ │
│  │  - 15:30: 장 마감 후 데이터 수집                         │ │
│  │  - 16:00: 기술적 지표 계산                               │ │
│  │  - 16:30: 시그널 감지 및 알림                            │ │
│  │  - 23:00: 데이터베이스 정리                              │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 인증 및 권한 관리

```
┌─────────────────────────────────────────────────────────────────┐
│                      JWT Authentication Flow                    │
└─────────────────────────────────────────────────────────────────┘

(1) 사용자 로그인
    ↓
┌──────────────────────────────────────┐
│  POST /api/v1/users/token            │
│  Body: {username, password}          │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  1. 비밀번호 검증 (bcrypt)           │
│     verify_password(plain, hashed)   │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  2. JWT 토큰 생성                    │
│     Algorithm: HS256                 │
│     Expiration: 24시간               │
│     Payload: {sub: username}         │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  3. 토큰 반환                        │
│  Response:                           │
│  {                                   │
│    access_token: "eyJ0eXAi...",      │
│    token_type: "bearer"              │
│  }                                   │
└──────────────────────────────────────┘

(2) 인증이 필요한 API 호출
    ↓
┌──────────────────────────────────────┐
│  GET /api/v1/users/dashboard         │
│  Header:                             │
│    Authorization: Bearer {token}     │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  1. 토큰 추출                        │
│     get_current_user(token)          │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  2. 토큰 검증                        │
│     jwt.decode(token, SECRET_KEY)    │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  3. 사용자 조회 (DB)                 │
│     User.query(username)             │
└──────────────┬───────────────────────┘
               ↓
┌──────────────────────────────────────┐
│  4. 비즈니스 로직 실행               │
│     (포트폴리오 조회 등)             │
└──────────────────────────────────────┘
```

---

## 4. 데이터베이스 아키텍처

### 4.1 데이터베이스 구성

```
┌─────────────────────────────────────────────────────────────────┐
│                   Database Architecture                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  PostgreSQL 14 + TimescaleDB Extension                   │
│  Port: 5435 (host) → 5432 (container)                    │
│  Database: stocktrading                                  │
│  User: admin / Password: admin123                        │
└──────────────────────────────────────────────────────────┘
         │
         ├─ ★ TimescaleDB Hypertables (시계열 최적화)
         │  └─ stock_prices (partitioned by date)
         │  └─ technical_indicators (partitioned by date)
         │
         ├─ Regular Tables (일반 테이블)
         │  ├─ users
         │  ├─ stocks
         │  ├─ portfolios
         │  ├─ trading_signals
         │  ├─ trading_positions
         │  └─ ...
         │
         └─ Indexes (인덱스 전략)
            ├─ (ticker, date) - 복합 인덱스
            ├─ (user_id) - 사용자 데이터
            └─ (created_at) - 시계열 정렬

┌──────────────────────────────────────────────────────────┐
│  Redis                                                   │
│  Port: 6380 (host) → 6379 (container)                    │
└──────────────────────────────────────────────────────────┘
         │
         ├─ Celery Broker (메시지 큐)
         │  └─ celery:task:{task_id}
         │
         ├─ Celery Result Backend (작업 결과)
         │  └─ celery-task-meta-{task_id}
         │
         └─ Cache (API 응답 캐싱)
            └─ stock:price:{ticker}:{date}

┌──────────────────────────────────────────────────────────┐
│  InfluxDB 2.7                                            │
│  Port: 8086                                              │
│  Organization: stocktrading                              │
│  Bucket: metrics                                         │
└──────────────────────────────────────────────────────────┘
         │
         ├─ API Performance Metrics
         │  └─ api_response_time (measurement)
         │
         ├─ Signal Generation Metrics
         │  └─ signal_count (measurement)
         │
         └─ System Metrics
            └─ db_query_time (measurement)
```

### 4.2 ER 다이어그램 (Entity-Relationship Diagram)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Core Domain Model                                │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────┐
│       users           │
├───────────────────────┤
│ PK  id                │
│ UK  username          │──┐
│ UK  email             │  │
│     password_hash     │  │ 1
│     initial_capital   │  │
│     current_assets    │  │
│     created_at        │  │
│     updated_at        │  │
└───────────────────────┘  │
                           │
                           │ *
                    ┌──────┴──────────┐
                    │                 │
         ┌──────────▼────────┐  ┌─────▼────────────┐
         │   portfolios      │  │  user_alerts     │
         ├───────────────────┤  ├──────────────────┤
         │ PK  id            │  │ PK  id           │
         │ FK  user_id       │  │ FK  user_id      │
         │ FK  ticker        │  │     alert_type   │
         │     quantity      │  │     title        │
         │     buy_price     │  │     message      │
         │     sell_price    │  │     priority     │
         │     buy_date      │  │     is_read      │
         │     sell_date     │  │     created_at   │
         │     status        │  └──────────────────┘
         └───────────────────┘

┌───────────────────────────────────────────────────────────┐
│       stocks (종목 마스터)                                │
├───────────────────────────────────────────────────────────┤
│ PK  ticker (String 10)                                    │
│     company_name                                          │
│     sector                                                │
│     industry                                              │
│     market_type (KOSPI/KOSDAQ)                            │
│     isin_code                                             │
│ FK  sector_id ──┐                                         │
│ FK  industry_id ──┐                                       │
│     listing_date  │                                       │
│     is_active     │                                       │
│     updated_at    │                                       │
└───────────────────┼───────────────────────────────────────┘
                    │
         ┌──────────┴─────────┬─────────────┬──────────────┐
         │                    │             │              │
         ▼ *                  ▼ *           ▼ *            ▼ *
┌─────────────────┐  ┌──────────────────┐  ┌─────────────┐  ┌────────────────┐
│ stock_prices    │  │ technical_       │  │ stock_      │  │ investor_      │
│ ★ Hypertable    │  │ indicators       │  │ fundamentals│  │ trades         │
├─────────────────┤  │ ★ Hypertable     │  ├─────────────┤  ├────────────────┤
│ PK ticker       │  ├──────────────────┤  │ PK ticker   │  │ PK ticker      │
│ PK date         │  │ PK ticker        │  │ PK date     │  │ PK date        │
│    open_price   │  │ PK date          │  │    per      │  │    foreign_buy │
│    high_price   │  │    rsi           │  │    pbr      │  │    foreign_sell│
│    low_price    │  │    macd          │  │    eps      │  │    instit_buy  │
│    close_price  │  │    macd_signal   │  │    bps      │  │    instit_sell │
│    volume       │  │    macd_histogram│  │    roe      │  │    indiv_buy   │
│    created_at   │  │    stoch_k       │  │    roa      │  │    indiv_sell  │
└─────────────────┘  │    stoch_d       │  │    debt_ratio│ └────────────────┘
                     │    ma_20         │  │    dividend_y│
   ↑ TimescaleDB     │    ma_50         │  │    created_at│
   partitioned by    │    ma_200        │  └─────────────┘
   date (automatic)  │    bollinger_up  │
                     │    bollinger_mid │
                     │    bollinger_low │
                     │    ichimoku_*    │
                     │    created_at    │
                     └──────────────────┘

┌───────────────────────────────────────────────────────────┐
│       Trading Domain (자동매매)                           │
└───────────────────────────────────────────────────────────┘

┌─────────────────────────┐         ┌──────────────────────┐
│ trading_signals         │         │ trading_positions    │
├─────────────────────────┤         ├──────────────────────┤
│ PK  id                  │         │ PK  id               │
│ FK  ticker              │────┐    │ FK  ticker           │
│     signal_type         │    │    │     buy_price        │
│     (BUY/SELL/STOP_LOSS)│    │    │     sell_price       │
│     signal_date         │    │    │     quantity         │
│     current_price       │    │    │     target_price     │
│     rsi                 │    │    │     stop_loss_price  │
│     ma_20               │    │    │     status           │
│     ma_50               │    │    │     (open/closed)    │
│     reason              │    │    │     buy_date         │
│     is_processed        │    │    │     sell_date        │
│     created_at          │    │    │     profit_loss      │
└─────────────────────────┘    │    │     profit_loss_pct  │
                               │    │     notes            │
                               │    │     created_at       │
                               │    └──────────────────────┘
                               │
                               └─── 동일 ticker 연관

┌─────────────────────────────────────────────────────────┐
│       Classification (분류 체계)                        │
└─────────────────────────────────────────────────────────┘

┌──────────────┐
│   sectors    │
├──────────────┤
│ PK  id       │──┐
│     name     │  │ 1
│     desc     │  │
└──────────────┘  │
                  │ *
          ┌───────▼─────────┐
          │   industries    │
          ├─────────────────┤
          │ PK  id          │
          │ FK  sector_id   │
          │     name        │
          │     desc        │
          └─────────────────┘
```

### 4.3 주요 테이블 상세

#### (1) stock_prices (TimescaleDB Hypertable)

```sql
CREATE TABLE stock_prices (
    ticker VARCHAR(10) NOT NULL,
    date DATE NOT NULL,
    open_price DECIMAL(10,2),
    high_price DECIMAL(10,2),
    low_price DECIMAL(10,2),
    close_price DECIMAL(10,2) NOT NULL,
    volume INTEGER,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (ticker, date),
    FOREIGN KEY (ticker) REFERENCES stocks(ticker)
);

-- TimescaleDB 시계열 테이블로 변환
SELECT create_hypertable('stock_prices', 'date',
    chunk_time_interval => INTERVAL '1 month');

-- 인덱스
CREATE INDEX idx_stock_prices_ticker ON stock_prices(ticker);
CREATE INDEX idx_stock_prices_date ON stock_prices(date DESC);

-- 특징:
-- - 자동 파티셔닝 (월별 청크)
-- - 시계열 데이터 압축
-- - 빠른 범위 쿼리 (date range)
-- - 예상 데이터 규모: 3,000 종목 × 10년 × 250일 = 750만 rows
```

#### (2) technical_indicators (TimescaleDB Hypertable)

```sql
CREATE TABLE technical_indicators (
    ticker VARCHAR(10) NOT NULL,
    date DATE NOT NULL,
    -- 모멘텀 지표
    rsi DECIMAL(6,2),                    -- 0~100
    stoch_k DECIMAL(6,2),                -- 0~100
    stoch_d DECIMAL(6,2),                -- 0~100
    -- 추세 지표
    macd DECIMAL(10,4),
    macd_signal DECIMAL(10,4),
    macd_histogram DECIMAL(10,4),
    ma_20 DECIMAL(10,2),
    ma_50 DECIMAL(10,2),
    ma_200 DECIMAL(10,2),
    -- 변동성 지표
    bollinger_upper DECIMAL(10,2),
    bollinger_middle DECIMAL(10,2),
    bollinger_lower DECIMAL(10,2),
    -- 일목균형표
    ichimoku_tenkan DECIMAL(10,2),       -- 전환선
    ichimoku_kijun DECIMAL(10,2),        -- 기준선
    ichimoku_senkou_a DECIMAL(10,2),     -- 선행스팬 A
    ichimoku_senkou_b DECIMAL(10,2),     -- 선행스팬 B
    ichimoku_chikou DECIMAL(10,2),       -- 후행스팬
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (ticker, date),
    FOREIGN KEY (ticker) REFERENCES stocks(ticker)
);

SELECT create_hypertable('technical_indicators', 'date',
    chunk_time_interval => INTERVAL '1 month');
```

#### (3) trading_signals (매매 시그널)

```sql
CREATE TABLE trading_signals (
    id SERIAL PRIMARY KEY,
    ticker VARCHAR(10) NOT NULL,
    signal_type VARCHAR(20) NOT NULL,    -- BUY, SELL, STOP_LOSS
    signal_date DATE NOT NULL,
    current_price DECIMAL(10,2),
    target_price DECIMAL(10,2),
    stop_loss_price DECIMAL(10,2),
    -- 시그널 근거 지표
    rsi DECIMAL(6,2),
    ma_20 DECIMAL(10,2),
    ma_50 DECIMAL(10,2),
    reason TEXT,                         -- 시그널 이유
    is_processed BOOLEAN DEFAULT FALSE,  -- 처리 여부
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    FOREIGN KEY (ticker) REFERENCES stocks(ticker)
);

CREATE INDEX idx_signals_ticker_date ON trading_signals(ticker, signal_date DESC);
CREATE INDEX idx_signals_type ON trading_signals(signal_type);
CREATE INDEX idx_signals_unprocessed ON trading_signals(is_processed)
    WHERE is_processed = FALSE;
```

### 4.4 데이터 보존 정책 (Retention Policy)

```sql
-- TimescaleDB 데이터 보존 정책
-- 3년 이상 오래된 데이터 자동 압축
SELECT add_retention_policy('stock_prices', INTERVAL '3 years');
SELECT add_retention_policy('technical_indicators', INTERVAL '3 years');

-- 압축 정책 (1개월 이상 오래된 데이터)
SELECT add_compression_policy('stock_prices', INTERVAL '1 month');
SELECT add_compression_policy('technical_indicators', INTERVAL '1 month');
```

### 4.5 쿼리 최적화 전략

```sql
-- (1) 최근 30일 주가 조회 (인덱스 활용)
SELECT * FROM stock_prices
WHERE ticker = '005930'
  AND date >= CURRENT_DATE - INTERVAL '30 days'
ORDER BY date DESC;

-- (2) RSI 과매도 종목 검색 (복합 인덱스)
SELECT ti.ticker, ti.rsi, sp.close_price
FROM technical_indicators ti
JOIN stock_prices sp ON ti.ticker = sp.ticker AND ti.date = sp.date
WHERE ti.date = (SELECT MAX(date) FROM technical_indicators)
  AND ti.rsi < 30
  AND ti.ma_20 > ti.ma_50
ORDER BY ti.rsi ASC
LIMIT 20;

-- (3) 포트폴리오 수익률 계산 (JOIN 최소화)
SELECT
    p.ticker,
    p.quantity,
    p.buy_price,
    sp.close_price AS current_price,
    (sp.close_price - p.buy_price) * p.quantity AS profit_loss,
    ((sp.close_price - p.buy_price) / p.buy_price) * 100 AS profit_loss_pct
FROM portfolios p
JOIN LATERAL (
    SELECT close_price
    FROM stock_prices
    WHERE ticker = p.ticker
    ORDER BY date DESC
    LIMIT 1
) sp ON true
WHERE p.user_id = 1 AND p.status = 'holding';
```

---

## 5. UI 아키텍처

### 5.1 프론트엔드 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                      Frontend Architecture                      │
│                   (Client-Side Rendering)                       │
└─────────────────────────────────────────────────────────────────┘

기술 스택:
  - HTML5 + CSS3 (Responsive Design)
  - Bootstrap 5.1.3 (UI Framework)
  - Vanilla JavaScript (ES6+)
  - Chart.js (데이터 시각화)
  - Font Awesome 6.0 (아이콘)

┌─────────────────────────────────────────────────────────────────┐
│                        UI Components                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. index.html - 메인 대시보드                           │  │
│  │  ────────────────────────────────────────────────────────│  │
│  │  위치: frontend/index.html                              │  │
│  │                                                          │  │
│  │  레이아웃 구조:                                          │  │
│  │  ┌────────────────────────────────────────────────┐     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  Navbar (사용자 정보, 로그아웃)            │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  로그인/회원가입 섹션 (미인증 시)         │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  탭 네비게이션                             │ │     │  │
│  │  │ │  ├─ 대시보드                                │ │     │  │
│  │  │ │  ├─ 종목 검색                               │ │     │  │
│  │  │ │  ├─ 포트폴리오                              │ │     │  │
│  │  │ │  └─ 시그널                                  │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  탭 콘텐츠 영역                            │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ 대시보드 탭:                         │ │ │     │  │
│  │  │ │  │ - 총 자산                            │ │ │     │  │
│  │  │ │  │ - 수익률                             │ │ │     │  │
│  │  │ │  │ - 보유 종목 수                       │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ 종목 검색 탭:                        │ │ │     │  │
│  │  │ │  │ - 티커 입력                          │ │ │     │  │
│  │  │ │  │ - Chart.js 주가 차트                 │ │ │     │  │
│  │  │ │  │ - 기술적 지표 카드 (RSI/MACD/MA)     │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ 포트폴리오 탭:                       │ │ │     │  │
│  │  │ │  │ - 보유 종목 리스트                   │ │ │     │  │
│  │  │ │  │ - 매수/매도 버튼                     │ │ │     │  │
│  │  │ │  │ - 수익률 계산 (실시간)               │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  └────────────────────────────────────────────────┘     │  │
│  │                                                          │  │
│  │  주요 기능:                                              │  │
│  │  - JWT 기반 인증 (LocalStorage 저장)                    │  │
│  │  - 탭 기반 SPA 구조                                     │  │
│  │  - Chart.js Candlestick 차트                            │  │
│  │  - Bootstrap Modal (매수/매도 다이얼로그)               │  │
│  │                                                          │  │
│  │  JavaScript 모듈:                                        │  │
│  │  - app.js (메인 로직)                                   │  │
│  │  - API 호출 함수                                        │  │
│  │  - 차트 렌더링 함수                                     │  │
│  │  - 이벤트 핸들러                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  2. rsi_ma_strategy.html - RSI+MA 매매 시그널 UI        │  │
│  │  ────────────────────────────────────────────────────────│  │
│  │  위치: backend/frontend/rsi_ma_strategy.html            │  │
│  │                                                          │  │
│  │  레이아웃 구조:                                          │  │
│  │  ┌────────────────────────────────────────────────┐     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  헤더: "📊 RSI+MA 상승전략"                │ │     │  │
│  │  │ │  - 전략 설명                                │ │     │  │
│  │  │ │  - 조건: RSI<30 AND MA20>MA50              │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  컨트롤 패널                                │ │     │  │
│  │  │ │  ├─ 날짜 선택 (Date Picker)                 │ │     │  │
│  │  │ │  ├─ 조회 버튼                               │ │     │  │
│  │  │ │  └─ 알림 전송 버튼 (카카오/Slack)          │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  시그널 통계 카드                           │ │     │  │
│  │  │ │  ├─ 총 시그널 수                            │ │     │  │
│  │  │ │  ├─ 평균 RSI                                │ │     │  │
│  │  │ │  └─ 예상 수익률                             │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  시그널 테이블 (정렬/필터링 가능)          │ │     │  │
│  │  │ │  ├─ Ticker                                  │ │     │  │
│  │  │ │  ├─ 종목명                                  │ │     │  │
│  │  │ │  ├─ 현재가                                  │ │     │  │
│  │  │ │  ├─ RSI                                     │ │     │  │
│  │  │ │  ├─ MA20/MA50                               │ │     │  │
│  │  │ │  └─ 액션 버튼 (상세보기)                   │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  알림 설정 모달                             │ │     │  │
│  │  │ │  ├─ 카카오톡 Access Token 입력             │ │     │  │
│  │  │ │  ├─ Slack Webhook URL 입력                 │ │     │  │
│  │  │ │  └─ 전송 버튼                               │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  └────────────────────────────────────────────────┘     │  │
│  │                                                          │  │
│  │  주요 기능:                                              │  │
│  │  - 실시간 시그널 조회 (GET /api/trading/signals/buy)    │  │
│  │  - 테이블 정렬 (RSI, 종목명, 현재가)                    │  │
│  │  - 멀티채널 알림 전송 (POST /notifications/send-signals)│  │
│  │  - 반응형 디자인 (모바일 대응)                          │  │
│  │                                                          │  │
│  │  CSS 특징:                                               │  │
│  │  - Gradient 배경 (#667eea → #764ba2)                    │  │
│  │  - Card Shadow (Material Design)                        │  │
│  │  - Hover 효과                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  3. screening.html - 종목 스크리닝 UI                    │  │
│  │  ────────────────────────────────────────────────────────│  │
│  │  위치: backend/frontend/screening.html                  │  │
│  │                                                          │  │
│  │  레이아웃 구조:                                          │  │
│  │  ┌────────────────────────────────────────────────┐     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  헤더: "📈 주식 스크리닝 & 백테스팅"       │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  전략 템플릿 섹션                           │ │     │  │
│  │  │ │  ├─ 기본 전략 목록                          │ │     │  │
│  │  │ │  │  • 모멘텀 돌파 전략                      │ │     │  │
│  │  │ │  │  • 과매도 반등 전략                      │ │     │  │
│  │  │ │  │  • 안정적 추세 전략                      │ │     │  │
│  │  │ │  └─ 사용자 정의 전략 (저장/불러오기)       │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  스크리닝 조건 설정                         │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ 조건 1:                              │ │ │     │  │
│  │  │ │  │ - 지표 선택: [RSI ▼]                 │ │ │     │  │
│  │  │ │  │ - 조건: [<] [30]                     │ │ │     │  │
│  │  │ │  │ - Lookback: [1일]                    │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ 조건 2:                              │ │ │     │  │
│  │  │ │  │ - 지표 선택: [MACD ▼]                │ │ │     │  │
│  │  │ │  │ - 조건: [Golden Cross]               │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ │  [+ 조건 추가] [조건 초기화]               │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  기간 설정                                  │ │     │  │
│  │  │ │  - 시작일: [2025-01-01]                     │ │     │  │
│  │  │ │  - 종료일: [2025-10-06]                     │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  액션 버튼                                  │ │     │  │
│  │  │ │  [🔍 스크리닝 실행] [📊 백테스팅 실행]      │ │     │  │
│  │  │ │  [💾 전략 저장]                             │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  결과 테이블                                │ │     │  │
│  │  │ │  - 조건 충족 종목 리스트                   │ │     │  │
│  │  │ │  - 지표 값 표시                            │ │     │  │
│  │  │ │  - 정렬/필터링 기능                        │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  백테스팅 결과                              │ │     │  │
│  │  │ │  - 총 수익률                               │ │     │  │
│  │  │ │  - MDD (최대 낙폭)                         │ │     │  │
│  │  │ │  - 승률                                    │ │     │  │
│  │  │ │  - Sharpe Ratio                            │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  └────────────────────────────────────────────────┘     │  │
│  │                                                          │  │
│  │  주요 기능:                                              │  │
│  │  - 동적 조건 추가/제거                                  │  │
│  │  - 전략 템플릿 JSON 저장/로드                           │  │
│  │  - 스크리닝 실행 (POST /api/v1/screening/screen)        │  │
│  │  - 백테스팅 실행 (POST /api/v1/screening/backtest)      │  │
│  │  - 결과 CSV 다운로드                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  4. stock_grid.html - 종목 관리 그리드                   │  │
│  │  ────────────────────────────────────────────────────────│  │
│  │  위치: backend/frontend/stock_grid.html                 │  │
│  │                                                          │  │
│  │  레이아웃 구조:                                          │  │
│  │  ┌────────────────────────────────────────────────┐     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  Navbar: "HSL 주식 시스템"                 │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  필터 카드                                  │ │     │  │
│  │  │ │  ├─ 검색: [Ticker/종목명]                   │ │     │  │
│  │  │ │  ├─ 시장: [전체/KOSPI/KOSDAQ]               │ │     │  │
│  │  │ │  ├─ 섹터: [전체/IT/금융/...]                │ │     │  │
│  │  │ │  └─ 국가: [KR/US/JP/CN]                     │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  데이터 그리드 (정렬 가능)                  │ │     │  │
│  │  │ │  ┌──────────────────────────────────────┐ │ │     │  │
│  │  │ │  │ Ticker │ 종목명 │ 현재가 │ 등락률 │ │ │     │  │
│  │  │ │  │ RSI │ MACD │ 시그널 │ 액션      │ │ │     │  │
│  │  │ │  ├──────────────────────────────────────┤ │ │     │  │
│  │  │ │  │ 005930 │ 삼성전자 │ 70,000 │ +2.1%│ │ │     │  │
│  │  │ │  │ 45.2 │ 120.5 │ 중립 │ [상세] │ │ │     │  │
│  │  │ │  └──────────────────────────────────────┘ │ │     │  │
│  │  │ │  (클릭 시 컬럼별 정렬)                     │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  │ ┌────────────────────────────────────────────┐ │     │  │
│  │  │ │  페이지네이션                               │ │     │  │
│  │  │ │  [◀] 1 2 3 4 5 ... 50 [▶]                  │ │     │  │
│  │  │ └────────────────────────────────────────────┘ │     │  │
│  │  └────────────────────────────────────────────────┘     │  │
│  │                                                          │  │
│  │  주요 기능:                                              │  │
│  │  - 실시간 필터링 (클라이언트 사이드)                    │  │
│  │  - 컬럼별 정렬 (오름차순/내림차순)                      │  │
│  │  - 페이지네이션 (100개씩)                               │  │
│  │  - 시그널 색상 표시 (매수=녹색, 매도=적색, 중립=회색)   │  │
│  │  - Sticky Header (스크롤 시 헤더 고정)                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  5. Grafana Dashboards (별도 포트 3000)                  │  │
│  │  ────────────────────────────────────────────────────────│  │
│  │  주요 대시보드:                                          │  │
│  │  - Enhanced Stock Analysis Dashboard                     │  │
│  │    • Candlestick 차트 (OHLCV)                            │  │
│  │    • 지표 오버레이 (MA, Bollinger Bands)                │  │
│  │    • RSI/MACD/Stochastic 서브 차트                       │  │
│  │    • 거래량 차트                                         │  │
│  │                                                          │  │
│  │  - Technical Indicators Dashboard                        │  │
│  │    • 다중 종목 비교                                      │  │
│  │    • 시그널 타임라인                                     │  │
│  │    • 백테스팅 결과 시각화                                │  │
│  │                                                          │  │
│  │  데이터 소스:                                            │  │
│  │  - PostgreSQL (stock_prices, technical_indicators)       │  │
│  │  - InfluxDB (metrics)                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 UI-Backend 통신 플로우

```
┌───────────────────────────────────────────────────────────────┐
│              Frontend-Backend Communication                   │
└───────────────────────────────────────────────────────────────┘

(1) 로그인 플로우
    ┌─────────────┐
    │   User      │
    └──────┬──────┘
           │ 1. 입력 (username, password)
           ↓
    ┌─────────────────────────┐
    │  Login Form (HTML)      │
    └──────┬──────────────────┘
           │ 2. Submit Event
           ↓
    ┌─────────────────────────┐
    │  JavaScript (app.js)    │
    │  - fetch() API 호출     │
    └──────┬──────────────────┘
           │ 3. POST /api/v1/users/token
           ↓
    ┌─────────────────────────┐
    │  FastAPI Backend        │
    │  - JWT 생성             │
    └──────┬──────────────────┘
           │ 4. Response {access_token}
           ↓
    ┌─────────────────────────┐
    │  JavaScript             │
    │  - localStorage.setItem │
    │    ('token', token)     │
    └──────┬──────────────────┘
           │ 5. UI 업데이트
           ↓
    ┌─────────────────────────┐
    │  Dashboard 표시         │
    └─────────────────────────┘

(2) 시그널 조회 플로우
    ┌─────────────┐
    │   User      │
    │  (버튼 클릭)│
    └──────┬──────┘
           │ 1. "시그널 조회" 클릭
           ↓
    ┌─────────────────────────┐
    │  JavaScript             │
    │  - const token =        │
    │    localStorage.getItem │
    └──────┬──────────────────┘
           │ 2. GET /api/trading/signals/buy
           │    Header: Authorization: Bearer {token}
           ↓
    ┌─────────────────────────┐
    │  FastAPI Backend        │
    │  - JWT 검증             │
    │  - DB 쿼리 실행         │
    └──────┬──────────────────┘
           │ 3. Response {signals: [...]}
           ↓
    ┌─────────────────────────┐
    │  JavaScript             │
    │  - renderSignalsTable() │
    └──────┬──────────────────┘
           │ 4. DOM 업데이트
           ↓
    ┌─────────────────────────┐
    │  <table> 렌더링         │
    │  - Ticker, RSI, MA      │
    └─────────────────────────┘

(3) 차트 렌더링 플로우
    ┌─────────────┐
    │   User      │
    │ (종목 입력) │
    └──────┬──────┘
           │ 1. Ticker 입력 (예: 005930)
           ↓
    ┌─────────────────────────┐
    │  JavaScript             │
    └──────┬──────────────────┘
           │ 2. GET /api/stocks/prices?ticker=005930
           ↓
    ┌─────────────────────────┐
    │  FastAPI Backend        │
    │  - stock_prices 조회    │
    │  - technical_indicators │
    └──────┬──────────────────┘
           │ 3. Response {prices: [...], indicators: [...]}
           ↓
    ┌─────────────────────────┐
    │  JavaScript             │
    │  - Chart.js 초기화      │
    │  - new Chart(ctx, {     │
    │      type: 'candlestick'│
    │    })                   │
    └──────┬──────────────────┘
           │ 4. Canvas 렌더링
           ↓
    ┌─────────────────────────┐
    │  <canvas> 차트 표시     │
    │  - OHLCV Candlestick    │
    │  - MA Lines Overlay     │
    └─────────────────────────┘
```

### 5.3 반응형 디자인 전략

```
┌───────────────────────────────────────────────────────────────┐
│                   Responsive Design (Bootstrap 5)             │
└───────────────────────────────────────────────────────────────┘

브레이크포인트:
  - xs: < 576px   (모바일)
  - sm: ≥ 576px   (모바일 가로)
  - md: ≥ 768px   (태블릿)
  - lg: ≥ 992px   (데스크톱)
  - xl: ≥ 1200px  (대형 데스크톱)

레이아웃 전략:
  ┌────────────────────────────────────────────────────────┐
  │  Desktop (≥ 992px)                                     │
  │  ┌──────────────┬──────────────┬──────────────┐       │
  │  │  Card 1      │  Card 2      │  Card 3      │       │
  │  │  col-lg-4    │  col-lg-4    │  col-lg-4    │       │
  │  └──────────────┴──────────────┴──────────────┘       │
  └────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────┐
  │  Tablet (768px ~ 991px)                                │
  │  ┌──────────────┬──────────────┐                      │
  │  │  Card 1      │  Card 2      │                      │
  │  │  col-md-6    │  col-md-6    │                      │
  │  └──────────────┴──────────────┘                      │
  │  ┌─────────────────────────────┐                      │
  │  │  Card 3                     │                      │
  │  │  col-md-12                  │                      │
  │  └─────────────────────────────┘                      │
  └────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────┐
  │  Mobile (< 768px)                                      │
  │  ┌─────────────────────────────┐                      │
  │  │  Card 1                     │                      │
  │  │  col-12                     │                      │
  │  └─────────────────────────────┘                      │
  │  ┌─────────────────────────────┐                      │
  │  │  Card 2                     │                      │
  │  │  col-12                     │                      │
  │  └─────────────────────────────┘                      │
  │  ┌─────────────────────────────┐                      │
  │  │  Card 3                     │                      │
  │  │  col-12                     │                      │
  │  └─────────────────────────────┘                      │
  └────────────────────────────────────────────────────────┘

테이블 반응형:
  - Desktop: 전체 컬럼 표시
  - Mobile: 가로 스크롤 또는 중요 컬럼만 표시
  - CSS: .table-responsive 클래스 사용
```

---

## 6. 통신 프로토콜

### 6.1 REST API 설계 원칙

```
┌───────────────────────────────────────────────────────────────┐
│                      REST API Guidelines                      │
└───────────────────────────────────────────────────────────────┘

(1) HTTP Methods
    - GET: 리소스 조회 (Idempotent)
    - POST: 리소스 생성 (Non-Idempotent)
    - PUT: 리소스 전체 수정 (Idempotent)
    - PATCH: 리소스 일부 수정 (Idempotent)
    - DELETE: 리소스 삭제 (Idempotent)

(2) 상태 코드
    - 200 OK: 성공
    - 201 Created: 생성 성공
    - 400 Bad Request: 잘못된 요청
    - 401 Unauthorized: 인증 실패
    - 403 Forbidden: 권한 없음
    - 404 Not Found: 리소스 없음
    - 500 Internal Server Error: 서버 오류

(3) URL 구조
    /{service}/{version}/{resource}/{id}/{action}

    예시:
    - GET  /api/v1/users/123          (특정 사용자 조회)
    - GET  /api/v1/users/123/portfolio (사용자 포트폴리오)
    - POST /api/trading/signals/buy    (매수 시그널 조회)
    - POST /api/v1/screening/screen    (스크리닝 실행)

(4) Request/Response 형식
    Content-Type: application/json

    Request Body:
    {
      "ticker": "005930",
      "quantity": 10,
      "buy_price": 70000
    }

    Response Body:
    {
      "status": "success",
      "data": {
        "id": 123,
        "ticker": "005930",
        "quantity": 10
      },
      "message": "Portfolio created successfully"
    }

(5) 에러 응답
    {
      "status": "error",
      "error_code": "INVALID_TICKER",
      "message": "Invalid ticker symbol",
      "details": {
        "ticker": "005930",
        "allowed_pattern": "^[0-9]{6}$"
      }
    }
```

### 6.2 WebSocket (향후 확장)

```
┌───────────────────────────────────────────────────────────────┐
│                  WebSocket for Real-time Updates              │
└───────────────────────────────────────────────────────────────┘

현재: HTTP Polling (주기적 API 호출)
향후: WebSocket (실시간 Push)

WebSocket 엔드포인트 (계획):
  - ws://localhost:8000/ws/prices/{ticker}
    → 실시간 주가 업데이트

  - ws://localhost:8000/ws/signals
    → 실시간 매매 시그널 알림

  - ws://localhost:8000/ws/portfolio/{user_id}
    → 실시간 포트폴리오 수익률

구현 계획:
  - FastAPI WebSocket 지원
  - Redis Pub/Sub 활용
  - 클라이언트: JavaScript WebSocket API
```

---

## 7. 보안 아키텍처

### 7.1 인증 보안

```
┌───────────────────────────────────────────────────────────────┐
│                      Security Measures                        │
└───────────────────────────────────────────────────────────────┘

(1) 비밀번호 보안
    ┌─────────────────────────────────────────────┐
    │  bcrypt 해싱 알고리즘                       │
    │  - Cost Factor: 12 (2^12 iterations)        │
    │  - Salt: 자동 생성 (per-password)           │
    │  - Output: 60자 해시                        │
    │                                             │
    │  예시:                                      │
    │  plain: "mypassword123"                     │
    │  hash: "$2b$12$KIXxM..."                    │
    └─────────────────────────────────────────────┘

(2) JWT 토큰 보안
    ┌─────────────────────────────────────────────┐
    │  JWT (JSON Web Token)                       │
    │  - Algorithm: HS256 (HMAC SHA-256)          │
    │  - Secret Key: 환경변수 (32+ bytes)         │
    │  - Expiration: 24시간                       │
    │  - Payload: {sub: username, exp: timestamp} │
    │                                             │
    │  구조:                                      │
    │  Header.Payload.Signature                   │
    │  eyJ0eXAi.eyJzdWIi.SflKxwRJ                 │
    └─────────────────────────────────────────────┘

(3) HTTPS (프로덕션 환경)
    - SSL/TLS 인증서 (Let's Encrypt)
    - 모든 API 통신 암호화
    - HSTS (HTTP Strict Transport Security)

(4) CORS 정책
    - 허용된 Origin만 접근 가능
    - credentials: true (쿠키 전송 허용)
    - 허용 메서드: GET, POST, PUT, DELETE

(5) SQL Injection 방지
    - SQLAlchemy ORM (Parameterized Query)
    - 모든 사용자 입력 검증 (Pydantic)
```

### 7.2 API Rate Limiting (향후 구현)

```
┌───────────────────────────────────────────────────────────────┐
│                      Rate Limiting Strategy                   │
└───────────────────────────────────────────────────────────────┘

계획:
  - slowapi 라이브러리 사용
  - Redis 기반 카운터

제한:
  - 인증된 사용자: 100 requests/minute
  - 미인증 사용자: 20 requests/minute
  - 시그널 조회: 10 requests/minute

헤더:
  X-RateLimit-Limit: 100
  X-RateLimit-Remaining: 95
  X-RateLimit-Reset: 1696598400

초과 시 응답:
  HTTP 429 Too Many Requests
  {
    "error": "Rate limit exceeded",
    "retry_after": 60
  }
```

---

## 8. 배포 아키텍처

### 8.1 Docker Compose 구성

```
┌───────────────────────────────────────────────────────────────┐
│                    Docker Compose Services                    │
└───────────────────────────────────────────────────────────────┘

docker-compose.yml:

version: '3.8'

services:
  ┌──────────────────────────────────────────────────────────┐
  │  postgres (TimescaleDB)                                  │
  │  ────────────────────────────────────────────────────────│
  │  Image: timescale/timescaledb:latest-pg14               │
  │  Port: 5435:5432                                         │
  │  Volume: postgres_data:/var/lib/postgresql/data         │
  │  Environment:                                            │
  │    POSTGRES_DB=stocktrading                              │
  │    POSTGRES_USER=admin                                   │
  │    POSTGRES_PASSWORD=admin123                            │
  │  Health Check: pg_isready                                │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  redis                                                   │
  │  ────────────────────────────────────────────────────────│
  │  Image: redis:alpine                                     │
  │  Port: 6380:6379                                         │
  │  Command: redis-server --appendonly yes                  │
  │  Volume: redis_data:/data                                │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  influxdb                                                │
  │  ────────────────────────────────────────────────────────│
  │  Image: influxdb:2.7                                     │
  │  Port: 8086:8086                                         │
  │  Volume: influxdb_data:/var/lib/influxdb2               │
  │  Environment:                                            │
  │    DOCKER_INFLUXDB_INIT_MODE=setup                       │
  │    DOCKER_INFLUXDB_INIT_USERNAME=admin                   │
  │    DOCKER_INFLUXDB_INIT_ORG=stocktrading                 │
  │    DOCKER_INFLUXDB_INIT_BUCKET=metrics                   │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  grafana                                                 │
  │  ────────────────────────────────────────────────────────│
  │  Image: grafana/grafana:latest                           │
  │  Port: 3000:3000                                         │
  │  Volume: grafana_data:/var/lib/grafana                   │
  │  Depends: postgres, influxdb                             │
  │  Environment:                                            │
  │    GF_SECURITY_ADMIN_PASSWORD=admin                      │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  backend (FastAPI)                                       │
  │  ────────────────────────────────────────────────────────│
  │  Build: ./backend                                        │
  │  Port: 8000:8000                                         │
  │  Volume: ./backend:/app (개발 모드)                      │
  │  Depends: postgres, redis, influxdb                      │
  │  Command: uvicorn main:app --host 0.0.0.0 --reload       │
  │  Environment: .env 파일 로드                             │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  celery-worker                                           │
  │  ────────────────────────────────────────────────────────│
  │  Build: ./backend                                        │
  │  Command: celery -A celery_app worker --loglevel=info    │
  │  Depends: redis, postgres                                │
  │  Environment: .env 파일 로드                             │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  celery-beat (Scheduler)                                 │
  │  ────────────────────────────────────────────────────────│
  │  Build: ./backend                                        │
  │  Command: celery -A celery_app beat --loglevel=info      │
  │  Depends: redis, postgres                                │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  airflow-webserver                                       │
  │  ────────────────────────────────────────────────────────│
  │  Image: apache/airflow:2.7.0                             │
  │  Port: 8080:8080                                         │
  │  Depends: postgres, redis                                │
  │  Environment:                                            │
  │    AIRFLOW__CORE__EXECUTOR=CeleryExecutor                │
  │    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql://...  │
  └──────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────┐
  │  airflow-scheduler                                       │
  │  ────────────────────────────────────────────────────────│
  │  Image: apache/airflow:2.7.0                             │
  │  Command: scheduler                                      │
  │  Depends: airflow-init, postgres, redis                  │
  └──────────────────────────────────────────────────────────┘

networks:
  stock-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  influxdb_data:
  grafana_data:
```

### 8.2 프로덕션 배포 (AWS 예시)

```
┌───────────────────────────────────────────────────────────────┐
│              Production Deployment on AWS                     │
└───────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  Route 53 (DNS)                                        │
│  - Domain: trading.example.com                         │
└────────────┬───────────────────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────────────┐
│  CloudFront (CDN)                                      │
│  - Static Assets 캐싱                                  │
│  - SSL/TLS 인증서                                      │
└────────────┬───────────────────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────────────┐
│  Application Load Balancer (ALB)                       │
│  - HTTPS Listener (Port 443)                           │
│  - Health Check: /health                               │
└────────────┬───────────────────────────────────────────┘
             │
         ┌───┴────┬────────┬────────┐
         ↓        ↓        ↓        ↓
    ┌────────┬────────┬────────┬────────┐
    │  ECS   │  ECS   │  ECS   │  ECS   │
    │ Task 1 │ Task 2 │ Task 3 │ Task 4 │
    │(FastAPI│(FastAPI│(Celery │(Celery │
    │Backend)│Backend)│Worker) │Beat)   │
    └────────┴────────┴────────┴────────┘
         │
         ↓
┌────────────────────────────────────────────────────────┐
│  RDS PostgreSQL + TimescaleDB                          │
│  - Multi-AZ Deployment                                 │
│  - Automated Backups                                   │
│  - Read Replica                                        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  ElastiCache Redis                                     │
│  - Cluster Mode                                        │
│  - Multi-AZ Replication                                │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  S3 Bucket                                             │
│  - Static Files (HTML/CSS/JS)                          │
│  - Backup Files                                        │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  CloudWatch                                            │
│  - Logs: Application Logs                              │
│  - Metrics: CPU, Memory, Request Count                 │
│  - Alarms: Error Rate, Response Time                   │
└────────────────────────────────────────────────────────┘
```

### 8.3 CI/CD 파이프라인 (GitHub Actions)

```yaml
# .github/workflows/deploy.yml

name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Tests
        run: |
          docker-compose -f docker-compose.test.yml up --abort-on-container-exit

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image
        run: |
          docker build -t stock-trading-backend:${{ github.sha }} ./backend
      - name: Push to ECR
        run: |
          aws ecr get-login-password | docker login --username AWS --password-stdin
          docker push stock-trading-backend:${{ github.sha }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to ECS
        run: |
          aws ecs update-service --cluster production --service backend \
            --force-new-deployment
```

---

## 부록: 주요 설정 파일

### A. .env (환경변수)

```bash
# Database
DATABASE_URL=postgresql://admin:admin123@postgres:5432/stocktrading
REDIS_URL=redis://redis:6379/0

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# InfluxDB
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=your-token-here
INFLUXDB_ORG=stocktrading
INFLUXDB_BUCKET=metrics

# JWT
JWT_SECRET_KEY=your-secret-key-min-32-characters
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

# API Keys (Optional)
YAHOO_FINANCE_API_KEY=
KAKAO_REST_API_KEY=
```

### B. requirements.txt

```txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
celery==5.3.4
redis==5.0.1
influxdb-client==1.38.0
pandas==2.1.3
yfinance==0.2.32
pykrx==1.0.36
python-dotenv==1.0.0
```

---

## 문서 버전 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0 | 2025-10-06 | 초안 작성 | System |

---

**문서 끝**
