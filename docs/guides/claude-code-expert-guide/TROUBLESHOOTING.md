# 문제 해결 가이드

Claude Code 사용 중 발생하는 일반적인 문제와 해결 방법

## 🔴 긴급: 작업한 내용이 날아갔을 때

### 증상
- 기능이 사라짐
- 코드가 이전 상태로 돌아감
- 여러 날 작업이 없어짐

### 원인
1. Git 저장소가 없음
2. Checkpoint만 사용 (세션 종료 시 손실)
3. 큰 변경 후 되돌림

### 해결
지금부터 할 수 있는 것:

```bash
# 1. 즉시 현재 상태 저장
git init  # 아직 저장소가 없다면
git add .
git commit -m "Emergency save: Current state"

# 2. 백업 브랜치 생성
git branch backup-$(date +%Y%m%d-%H%M%S)
```

### 예방
```bash
# 매일 작업 시작 전
git add .
git commit -m "Daily checkpoint: $(date +%Y-%m-%d)"

# 작업 중간중간
git add .
git commit -m "wip: [진행 중인 작업]"
```

---

## ⚠️ Claude가 테스트를 수정해버림

### 증상
- 잘못된 구현이 테스트를 통과
- 테스트가 원래 의도와 다름
- 버그가 테스트에서 잡히지 않음

### 원인
Claude가 구현을 테스트에 맞추지 않고, 테스트를 구현에 맞춤

### 해결
```
사용자: "테스트를 절대 수정하지 마.
테스트는 스펙이야. 구현을 고쳐서 테스트를 통과시켜"
```

### 테스트 보호 방법
```bash
# 테스트 파일을 별도 커밋
git add tests/
git commit -m "test: Add tests for feature X"

# 이제 구현 작업
```

테스트가 수정되었다면:
```bash
# 테스트 파일만 되돌리기
git checkout HEAD -- tests/test_feature.py
```

---

## 🔄 변경사항이 너무 커서 리뷰가 불가능

### 증상
- Diff가 1000+ lines
- 여러 파일이 동시에 변경
- 무엇이 바뀌었는지 모르겠음

### 해결
```
사용자: "이 변경을 5개의 작은 커밋으로 나눠줘.
각 커밋은 독립적으로 작동해야 해"
```

또는 처음부터:
```
사용자: "/rewind로 변경 전으로 돌아가줘"

"이 작업을 5단계로 나눠줘. 각 단계는 200 lines 이하"
```

### 예방
```
"이 기능의 계획을 세워줘.
각 단계는 50-200 lines로 제한"
```

---

## 🐛 Pre-commit Hook이 계속 실패

### 증상
```
git commit
→ Running pre-commit checks...
→ Linting failed
→ Commit rejected
```

### 해결

#### Option 1: 자동 수정 (권장)
```bash
# Lint 오류 자동 수정
npm run lint:fix
# 또는
ruff check --fix .
# 또는
black .

# 다시 커밋
git add .
git commit -m "..."
```

#### Option 2: Claude에게 수정 요청
```
"Pre-commit hook이 실패했어.
hook이 요구하는 대로 코드를 수정해줘.
Hook은 절대 건너뛰지 마"
```

#### ❌ 하지 말 것
```bash
# 이렇게 하지 마세요!
git commit --no-verify
```

---

## 💭 Claude가 계획 없이 코딩 시작

### 증상
- 바로 파일 수정 시작
- 접근 방법을 설명하지 않음
- 중간에 방향 전환

### 해결
```
사용자: "Stop! 코드 작성하기 전에 계획을 먼저 보여줘"
```

### 예방
```
항상 요청 시:
"[기능]을 구현하기 전에, 3단계 계획을 세워줘.
계획 승인 후에 구현 시작"
```

CLAUDE.md에 명시:
```markdown
## Rules
- **ALWAYS** show plan before implementation
- **NEVER** start coding without approval
```

---

## 🔍 변경사항을 이해할 수 없음

### 증상
- Claude가 뭘 했는지 모르겠음
- 왜 이렇게 바꿨는지 모르겠음
- 부작용이 걱정됨

### 해결
```
사용자: "방금 변경한 내용을 설명해줘.
- 무엇을 변경했는지
- 왜 그렇게 했는지
- 기존 기능에 영향은 없는지"
```

### 예방
Plan Mode 사용:
```
[Shift+Tab x2]

"이 기능을 구현하는 방법을 설명해줘.
코드는 아직 수정하지 말고, 어떻게 할 건지만 설명"
```

---

## 📦 Context Window가 가득 참

### 증상
- 응답이 느려짐
- 이전 내용을 잊어버림
- "토큰 제한" 관련 오류

### 해결
```bash
# /clear 명령어 사용
```

```
사용자: "/clear"

# 필요한 컨텍스트만 다시 로드
"@CLAUDE.md와 @docs/plan.md를 참조해서 작업 계속"
```

### 예방
- 각 feature마다 /clear
- CLAUDE.md를 5000 토큰 이하로 유지
- 문서는 별도 파일로 분리

---

## ⏪ 되돌리기가 필요할 때

### Scenario 1: 방금 한 변경만 취소

```
사용자: "/rewind"

# 또는
사용자: "Esc Esc" (Rewind 메뉴)
```

### Scenario 2: 특정 시점으로 돌아가기

```bash
# Git log 확인
git log --oneline

# 특정 커밋으로 돌아가기
git reset --hard abc1234
```

### Scenario 3: 파일 하나만 되돌리기

```bash
# 특정 파일만 이전 상태로
git checkout HEAD -- path/to/file.py
```

### Scenario 4: 실험적 변경 취소

```
[작업 전 checkpoint 자동 생성됨]

# 실험 실패 시
"Esc Esc" → Rewind 메뉴 → 이전 체크포인트 선택
```

---

## 🔐 민감한 정보가 커밋됨

### 증상
```
.env 파일 커밋됨
API 키가 소스코드에
비밀번호가 Git에 기록됨
```

### 즉시 조치

```bash
# 1. 최근 커밋에서 제거
git reset HEAD~1
git reset .env
git commit -m "..."

# 2. .gitignore에 추가
echo ".env" >> .gitignore
echo "*.key" >> .gitignore
git add .gitignore
git commit -m "chore: Add sensitive files to .gitignore"

# 3. 이미 푸시했다면
# → GitHub/GitLab에서 secret 즉시 변경
# → git-filter-branch로 히스토리에서 제거 (복잡함, 주의)
```

### 예방

CLAUDE.md에 명시:
```markdown
## Forbidden Files
Never commit:
- .env
- *.key
- credentials.json
- config/secrets.yml
```

Pre-commit hook:
```bash
#!/bin/bash
# .git/hooks/pre-commit

if git diff --cached --name-only | grep -E '\.(env|key)$'; then
    echo "ERROR: Attempting to commit sensitive files"
    exit 1
fi
```

---

## 🧪 테스트가 계속 실패

### 원인 1: 환경 문제

```bash
# 의존성 재설치
pip install -r requirements.txt
# 또는
npm install

# 캐시 삭제
rm -rf __pycache__/
# 또는
rm -rf node_modules/.cache
```

### 원인 2: 잘못된 구현

```
사용자: "모든 테스트를 실행하고, 실패 원인을 분석해줘.
그리고 최소한의 수정으로 통과시켜줘"
```

### 원인 3: 환경 변수

```bash
# 테스트용 .env.test 생성
cp .env.example .env.test

# 테스트 실행 시
ENV_FILE=.env.test pytest
```

---

## 📊 어떤 파일이 변경되었는지 모름

### 해결
```bash
# 변경된 파일 목록
git status

# 구체적 변경 내용
git diff

# 스테이징된 변경 내용
git diff --staged
```

Claude에게:
```
"방금 변경한 모든 파일 목록과 각 파일의 변경 요약을 보여줘"
```

---

## 🤔 Claude가 같은 실수를 반복

### 원인
Claude는 세션 간 기억하지 못함

### 해결
CLAUDE.md에 문서화:

```markdown
## Common Mistakes to Avoid

### ❌ Don't do this:
```python
# Claude가 자주 하는 실수
user = User.query.filter_by(id=id).first()
```

### ✅ Do this instead:
```python
# 올바른 방법
user = User.query.filter_by(id=id).first_or_404()
```

## Anti-Patterns
- Don't use SELECT *
- Don't mutate function arguments
- Don't catch generic exceptions
```

---

## 🚦 Claude가 응답을 멈춤

### 원인
- 너무 복잡한 요청
- 컨텍스트 너무 많음
- 네트워크 문제

### 해결
```
# Escape로 중단
Esc

# 더 작은 요청으로 나누기
"Step 1만 먼저 해줘"
```

---

## 빠른 참조

| 문제 | 해결 |
|------|------|
| 작업 날아감 | Git 커밋, 백업 브랜치 |
| 테스트 수정됨 | 명시적 금지, Git으로 되돌리기 |
| 변경 너무 큼 | /rewind, 작게 나누기 |
| Hook 실패 | 자동 수정, 절대 skip 금지 |
| 계획 없음 | Plan Mode, 명시적 요청 |
| 컨텍스트 가득 | /clear, 문서 분리 |
| 되돌리기 | /rewind, git reset |
| 민감 정보 | .gitignore, pre-commit hook |

---

## 도움이 더 필요하면

- [README.md](README.md) - 전체 가이드
- [QUICK-START.md](QUICK-START.md) - 빠른 시작
- 개별 가이드 파일들 참조

**기억하세요**: 문제가 생기면 일단 Git 커밋하고, 작게 나누고, 천천히 진행하세요.
