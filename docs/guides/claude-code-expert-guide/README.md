# Claude Code 전문가 가이드

며칠간의 작업을 한 순간에 잃지 않기 위한 실전 가이드입니다.

## 🚨 가장 중요한 3가지

### 1. 항상 Git부터 시작
```bash
cd my-project
git init
git add .
git commit -m "Initial commit before Claude Code work"
```

**이것만은 꼭**: 작업 시작 전 Git 저장소를 만들고 초기 커밋을 생성하세요.

### 2. 계획 먼저, 구현은 나중에
```
❌ "로그인 기능 만들어줘" → 바로 코딩 시작
✅ "로그인 기능의 3단계 계획을 세워줘" → 검토 → 승인 → 한 단계씩 구현
```

### 3. 작게, 자주, 확인
```
큰 변경 (1000 lines) = 위험
작은 변경 (50-200 lines) + 테스트 + 리뷰 + 커밋 = 안전
```

## 📚 가이드 구성

### 🔥 즉시 적용 가능 (필수)

1. **[01-git-create-branches-first.md](01-git-create-branches-first.md)** - 브랜치 먼저 만들기
2. **[16-ask-for-plan-first.md](16-ask-for-plan-first.md)** - 계획 먼저 요청
3. **[30-plan-mode-for-complex-changes.md](30-plan-mode-for-complex-changes.md)** - Plan Mode 사용법
4. **[31-write-tests-first-tdd.md](31-write-tests-first-tdd.md)** - TDD로 안전하게
5. **[69-small-changes-philosophy.md](69-small-changes-philosophy.md)** - 작은 변경 철학
6. **[97-claude-md-best-practices.md](97-claude-md-best-practices.md)** - CLAUDE.md 작성법

### Git 워크플로우 (1-15)

- **01** - 브랜치 먼저 만들기
- **02** - 커밋 메시지 생성
- **03** - 프로젝트 규칙 따르기
- **04** - Checkpoint vs Git 구분
- **05** - Git Hook 스킵하지 않기

### 프로젝트 관리 (16-29)

- **16** - 계획 먼저 요청하기 ⭐
- **17** - 큰 작업 나누기
- **22** - 작은 diff 유지
- **26** - 커스텀 slash 명령어
- **27** - CLAUDE.md 문서화

### 오류 방지 (30-44)

- **30** - Plan Mode 사용 ⭐
- **31** - TDD로 테스트 먼저 ⭐
- **38** - Pre-commit hook 설정
- **39** - 자동 검증 루프

### 점진적 개발 (69-76)

- **69** - 작은 변경 철학 ⭐
- **70** - 컨텍스트 관리
- **73** - Checkpoint + Rewind

### 컨텍스트 관리 (97-111)

- **97** - CLAUDE.md 모범 사례 ⭐
- **103** - /clear 자주 사용
- **105** - 세션 스코핑

## 🎯 상황별 가이드

### 😱 며칠 작업이 날아갔을 때

**지금부터 할 일:**

1. **Git 저장소 생성**
   ```bash
   cd stock-trading-system
   git init
   git config user.name "Your Name"
   git config user.email "your.email@example.com"
   git add .
   git commit -m "Baseline: Current working state"
   ```

2. **CLAUDE.md 작성**
   ```bash
   # 프로젝트 root에 생성
   # 기술 스택, 명령어, 규칙 문서화
   # 5000 토큰 이하로 유지
   ```

3. **작은 변경 시작**
   ```bash
   git checkout -b feature/first-safe-change
   # 작은 변경 하나만
   # 테스트 작성
   # 리뷰
   # 커밋
   git commit -m "feat: small safe change"
   ```

4. **반복**
   - 한 번에 하나씩
   - 항상 테스트
   - 자주 커밋

### 🚀 새 프로젝트 시작할 때

```bash
# 1. Git 초기화
git init
git config user.name "..."
git config user.email "..."

# 2. .gitignore 생성
cat > .gitignore <<EOF
.env
*.pyc
__pycache__/
node_modules/
.DS_Store
EOF

# 3. CLAUDE.md 생성
cat > CLAUDE.md <<EOF
# My Project

## Tech Stack
- ...

## Commands
- Run: ...
- Test: ...

## Code Style
- ...

## Git Workflow
- Never use --no-verify
- Always create feature branches
- Conventional commits
EOF

# 4. 초기 커밋
git add .
git commit -m "chore: Initial project setup"

# 5. Claude에게 작업 요청
```

### 🔧 기존 작업 중일 때

```
1. 현재 상태 커밋
   git add .
   git commit -m "wip: Current state before Claude work"

2. 브랜치 생성
   git checkout -b feature/new-work

3. 계획 요청
   "이 작업의 3단계 계획을 세워줘"

4. 한 단계씩
   - 구현
   - 테스트
   - 리뷰
   - 커밋

5. 완료 후 병합
   git checkout main
   git merge feature/new-work
```

### 🐛 버그 수정할 때

```
1. 재현 테스트 먼저
   "이 버그를 재현하는 테스트를 작성해줘"

2. 테스트 실패 확인
   pytest test_bug.py
   → FAILED ✓ (재현 성공)

3. 최소 수정
   "테스트를 통과하는 최소한의 수정을 해줘"

4. 테스트 통과 확인
   pytest test_bug.py
   → PASSED ✓

5. 커밋
   git commit -m "fix: bug description"
```

## 💡 Claude에게 요청하는 방법

### ❌ 피해야 할 요청

```
"전체 시스템을 만들어줘"
"이거 고쳐줘" (뭘 어떻게?)
"빨리 해줘" (테스트 생략)
"일단 돌아가게만" (기술 부채)
```

### ✅ 좋은 요청

```
"사용자 인증 기능을 3단계로 나눠서 계획을 세워줘.
각 단계는 200 lines 이하, 테스트 포함, 독립적으로 작동해야 해"

"이 함수의 테스트를 먼저 작성해줘.
⚠️ 중요: 구현은 아직 하지 마"

"@docs/plan.md의 Step 2를 구현해줘.
git hook을 절대 건너뛰지 말고,
테스트가 모두 통과하면 커밋해줘"
```

## 🛡️ 안전 장치

### 1. Pre-commit Hook 설정

```bash
# .git/hooks/pre-commit
#!/bin/bash

echo "Running pre-commit checks..."

# Tests
pytest tests/ || exit 1

# Linting
ruff check . || exit 1

# Type checking
mypy . || exit 1

echo "✓ All checks passed"
```

```bash
chmod +x .git/hooks/pre-commit
```

### 2. CLAUDE.md에 규칙 명시

```markdown
## Rules for Claude

- **NEVER** skip git hooks
- **NEVER** modify tests to match wrong implementation
- **ALWAYS** ask for plan approval before large changes
- **ALWAYS** keep diffs under 200 lines
- **ALWAYS** write tests before implementation
```

### 3. 작업 전 체크리스트

```
작업 시작 전:
[ ] Git 저장소 있음
[ ] 현재 상태 커밋됨
[ ] Feature 브랜치 생성
[ ] 계획 수립 완료
[ ] 계획 승인받음

각 단계 시작 전:
[ ] Checkpoint 생성
[ ] 작업 범위 명확

각 단계 완료 후:
[ ] 테스트 통과
[ ] Lint 통과
[ ] 리뷰 완료
[ ] 커밋
```

## 🎓 학습 순서

**1주차: 기초**
- Git 워크플로우 이해
- CLAUDE.md 작성
- 작은 변경 연습

**2주차: 안전성**
- TDD 연습
- Plan Mode 활용
- Pre-commit hook 설정

**3주차: 효율성**
- Slash 명령어 생성
- 컨텍스트 관리
- 반복 작업 자동화

**4주차: 숙련**
- 복잡한 프로젝트 적용
- 팀 워크플로우 구축
- 고급 기능 탐색

## 📖 더 읽을 거리

전체 200개 모범 사례는 개별 파일 참조:

- **Git 워크플로우**: 01-15번 파일
- **프로젝트 관리**: 16-29번 파일
- **오류 방지**: 30-44번 파일
- **소통 패턴**: 45-56번 파일
- **파일 관리**: 57-68번 파일
- **점진적 개발**: 69-76번 파일
- **검증 워크플로우**: 77-88번 파일
- **복구 전략**: 89-96번 파일
- **컨텍스트 관리**: 97-111번 파일

## 🔗 빠른 참조

```
계획이 필요할 때      → 16, 30
테스트가 필요할 때    → 31, 84
문제가 생겼을 때      → 89-96
처음 시작할 때        → 01, 97
큰 작업할 때          → 17, 22, 69
팀 협업할 때          → 112-124
```

## ⚡ 요약: 절대 잊지 말 것

1. **Git first, always**
2. **Plan before code**
3. **Small, tested, reviewed, committed**
4. **Never skip hooks**
5. **Document in CLAUDE.md**

이것만 지켜도 며칠간의 작업을 잃을 일은 없습니다.

---

**만든 이유**: 같은 실수를 반복하지 않기 위해
**대상**: Claude Code를 안전하게 사용하고 싶은 모든 개발자
**목표**: 여러분의 코드를 지키는 것
