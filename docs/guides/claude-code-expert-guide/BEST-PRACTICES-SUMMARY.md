# Claude Code 핵심 모범 사례 요약

## 🏆 Top 10 규칙

### 1. Git First, Always
```bash
# 모든 작업 시작 전
git init
git add .
git commit -m "Initial commit"
```

### 2. Branch Before Work
```bash
git checkout -b feature/your-feature
```

### 3. Plan Before Code
```
"[기능]을 3단계 계획으로 나눠줘"
→ 검토
→ 승인
→ 구현
```

### 4. Test Before Implementation (TDD)
```
"테스트 먼저 작성해줘. 구현은 아직 하지 마"
→ 테스트 실패 확인
→ 구현
→ 테스트 통과 확인
```

### 5. Small Diffs (50-200 lines)
```
한 번에 하나씩
독립적으로 완성
쉽게 리뷰 가능
```

### 6. Never Skip Hooks
```
❌ git commit --no-verify
✅ 문제 수정 후 다시 커밋
```

### 7. Review Everything
```
변경 전: 계획 리뷰
변경 후: 코드 리뷰
커밋 전: Diff 리뷰
```

### 8. Commit Often
```
작은 변경 → 테스트 → 커밋
작은 변경 → 테스트 → 커밋
작은 변경 → 테스트 → 커밋
```

### 9. Document in CLAUDE.md
```markdown
# 프로젝트 규칙, 패턴, 안티패턴
# 기술 스택, 명령어
# 세션 간 기억해야 할 것들
```

### 10. Use /clear Frequently
```
기능 완료 → /clear
새 기능 시작 → @CLAUDE.md 참조
```

---

## 📋 워크플로우 체크리스트

### 프로젝트 시작 시

```
[ ] Git 저장소 초기화
[ ] .gitignore 생성
[ ] CLAUDE.md 작성
[ ] Pre-commit hook 설정
[ ] 초기 커밋 생성
```

### 기능 개발 시

```
[ ] Feature 브랜치 생성
[ ] 계획 수립 및 승인
[ ] 테스트 작성
[ ] 구현
[ ] 테스트 통과 확인
[ ] 코드 리뷰
[ ] 커밋
[ ] (반복)
[ ] PR 생성
```

### 버그 수정 시

```
[ ] 버그 재현 테스트 작성
[ ] 테스트 실패 확인
[ ] 최소 수정
[ ] 테스트 통과 확인
[ ] 커밋
```

### 리팩토링 시

```
[ ] 기존 테스트 모두 통과 확인
[ ] Checkpoint 생성
[ ] 작은 단위로 리팩토링
[ ] 각 단계마다 테스트
[ ] 커밋
```

---

## 🎯 상황별 베스트 프랙티스

### 새 기능 추가

```
1. [Plan Mode] "이 기능의 설계를 분석해줘"
2. "3단계 구현 계획을 세워줘"
3. "Step 1: 테스트 작성"
4. "Step 1: 구현"
5. "Step 1: 커밋"
6. 2-5 반복
```

### 레거시 코드 수정

```
1. [Plan Mode] "이 코드를 분석해줘"
2. "영향 범위를 분석해줘"
3. "안전하게 수정하는 계획을 세워줘"
4. "기존 동작을 검증하는 테스트 작성"
5. "한 번에 하나씩 수정"
```

### 성능 최적화

```
1. "현재 성능을 측정하는 벤치마크 작성"
2. "병목 지점 분석"
3. "최적화 계획 (측정 가능한 목표)"
4. "한 번에 하나씩 최적화"
5. "각 단계마다 벤치마크로 검증"
```

### 대규모 마이그레이션

```
1. "마이그레이션 계획을 docs/migration.md에 작성"
2. "10-20단계로 세분화"
3. "각 단계는 독립적으로 배포 가능하게"
4. "한 단계 완료 → /clear → 다음 단계"
```

---

## 🚫 절대 하지 말 것

### ❌ Anti-Patterns

```
1. 계획 없이 큰 변경
   → 항상 계획 먼저

2. 테스트 없이 구현
   → TDD 사용

3. Git hook 건너뛰기
   → 문제 수정 후 재시도

4. 한 번에 많은 파일 수정
   → 50-200 lines씩

5. 테스트 수정해서 통과시키기
   → 구현을 수정

6. 커밋 메시지 "fix", "update"
   → 의미 있는 메시지

7. main 브랜치에 직접 커밋
   → Feature 브랜치 사용

8. CLAUDE.md 없이 작업
   → 세션마다 반복 설명

9. 변경사항 리뷰 없이 커밋
   → git diff로 확인

10. 긴 세션 계속 유지
    → /clear로 정리
```

---

## 💡 프로 팁

### Git 관련

```bash
# 매일 아침 습관
git pull
git checkout -b feature/today-work
git add . && git commit -m "Start of day checkpoint"

# 매일 저녁 습관
git add . && git commit -m "End of day checkpoint"
git push -u origin feature/today-work
```

### Claude 활용

```
# 복잡한 작업
"ultrathink" + [Shift+Tab x2] (Plan Mode)

# 반복 작업
Custom slash command 생성

# 큰 프로젝트
CLAUDE.md를 디렉토리별로 분리
```

### 테스트 전략

```python
# 테스트 우선순위
1. Critical path (핵심 기능)
2. Edge cases (경계 조건)
3. Error handling (오류 처리)
4. Happy path (정상 경로)
```

### 컨텍스트 관리

```
작은 기능 (1-2시간) → 한 세션
중간 기능 (반나절) → 2-3 세션 (/clear 사용)
큰 기능 (1일+) → docs/plan.md + 여러 세션
```

---

## 📊 성과 측정

### 좋은 신호

```
✅ 커밋이 작고 빈번 (하루 5-10회)
✅ 각 커밋이 독립적으로 작동
✅ 테스트 커버리지 >80%
✅ PR 리뷰 시간 <30분
✅ 버그 발견이 빠름
✅ 롤백이 안전하고 쉬움
```

### 나쁜 신호

```
❌ 커밋이 크고 드묾
❌ 리뷰에 1시간 이상
❌ 테스트가 자주 깨짐
❌ 무엇이 변경되었는지 모름
❌ 롤백이 두려움
❌ 같은 버그 반복
```

---

## 🎓 학습 곡선

### Week 1: 기초
- [ ] Git 워크플로우 숙달
- [ ] CLAUDE.md 작성
- [ ] 브랜치/커밋 습관화

### Week 2: 안전성
- [ ] TDD 연습
- [ ] Pre-commit hook 설정
- [ ] Plan Mode 활용

### Week 3: 효율성
- [ ] Slash 명령어 생성
- [ ] 작은 diff 습관화
- [ ] /clear 활용

### Week 4: 숙련
- [ ] 복잡한 프로젝트 적용
- [ ] 팀 규칙 정립
- [ ] 고급 기능 탐색

---

## 📈 진화 과정

```
Level 1: 생존
→ Git 사용, 백업 유지

Level 2: 안전
→ 브랜치, 작은 커밋, 테스트

Level 3: 효율
→ TDD, Plan Mode, /clear

Level 4: 숙련
→ 자동화, 팀 워크플로우, 최적화

Level 5: 마스터
→ 직관적 사용, 고급 패턴, 멘토링
```

---

## 🔗 추가 리소스

### 필수 파일

- [README.md](README.md) - 전체 개요
- [QUICK-START.md](QUICK-START.md) - 빠른 시작
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 문제 해결

### 주제별 가이드

- Git: 01-15
- Project Management: 16-29
- Error Prevention: 30-44
- Testing: 31, 84
- Context: 97-111

---

## ⚡ 기억해야 할 한 가지

**"Plan → Small → Test → Review → Commit"**

이 사이클만 지키면 됩니다.

작게, 자주, 안전하게.
