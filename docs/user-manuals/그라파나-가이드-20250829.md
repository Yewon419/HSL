# Grafana 모니터링 대시보드 사용자 매뉴얼

**작성일**: 2025-08-29  
**버전**: v1.0.0  
**대상**: 주식 투자 시뮬레이션 시스템 사용자

---

## 📋 목차

1. [Grafana 접속 방법](#-grafana-접속-방법)
2. [데이터소스 설정](#-데이터소스-설정)
3. [대시보드 생성 가이드](#-대시보드-생성-가이드)
4. [주요 차트 만들기](#-주요-차트-만들기)
5. [고급 설정 및 커스터마이징](#-고급-설정-및-커스터마이징)
6. [문제 해결](#-문제-해결)

---

## 🔑 Grafana 접속 방법

### 접속 정보

**URL:** http://localhost:3000

**로그인 계정:**
- **Username:** `admin`
- **Password:** `admin123`

### 접속 단계

1. **웹 브라우저**에서 http://localhost:3000 접속
2. **Username:** `admin` 입력
3. **Password:** `admin123` 입력  
4. **Log in** 버튼 클릭

### 첫 접속 시 초기 설정

- 비밀번호 변경을 묻는 경우 **Skip** 선택 (또는 새 비밀번호 설정)
- Welcome 화면에서 **Get started** 클릭

---

## 🔌 데이터소스 설정

### 1단계: 데이터소스 메뉴 접근

1. **왼쪽 사이드바**에서 **⚙️ Configuration** 클릭
2. **Data sources** 선택
3. **Add data source** 버튼 클릭

### 2단계: PostgreSQL 선택

1. 데이터소스 목록에서 **PostgreSQL** 클릭
2. 또는 검색창에 "PostgreSQL" 입력 후 선택

### 3단계: 연결 설정

**다음 정보를 정확히 입력:**

| 항목 | 값 |
|------|-----|
| **Name** | `PostgreSQL` |
| **Host** | `stock-db:5432` |
| **Database** | `stocktrading` |
| **User** | `admin` |
| **Password** | `admin123` |
| **SSL Mode** | `disable` |
| **Version** | `12+` (기본값) |

**⚠️ 중요 사항:**
- Host는 `localhost:5435`가 아닌 `stock-db:5432`입니다 (Docker 내부 네트워크)
- 모든 필드를 정확히 입력해야 합니다

### 4단계: 연결 테스트

1. **Save & test** 버튼 클릭
2. **"Database Connection OK"** 초록색 메시지 확인
3. 오류 발생 시 [문제 해결](#-문제-해결) 섹션 참고

---

## 📊 대시보드 생성 가이드

### 새 대시보드 만들기

1. **왼쪽 사이드바**에서 **➕ (Plus icon)** 클릭
2. **Dashboard** 선택
3. **Add new panel** 클릭

### 대시보드 기본 구성

**권장 대시보드 구성:**
- 주가 추이 차트 (Time series)
- RSI 지표 게이지 (Gauge)
- 포트폴리오 성과 테이블 (Table)
- 자산 배분 파이차트 (Pie chart)

---

## 📈 주요 차트 만들기

### 1. 주가 추이 차트 (Stock Price Trends)

#### 패널 설정
1. **Data source:** PostgreSQL 선택
2. **Query 탭**에서 다음 SQL 입력:

```sql
SELECT 
  date as time,
  CASE 
    WHEN ticker = '005930.KS' THEN 'Samsung Electronics'
    WHEN ticker = '000660.KS' THEN 'SK Hynix' 
    WHEN ticker = '035420.KS' THEN 'NAVER'
    WHEN ticker = 'AAPL' THEN 'Apple'
    WHEN ticker = 'NVDA' THEN 'NVIDIA'
    WHEN ticker = 'TSLA' THEN 'Tesla'
    WHEN ticker = 'MSFT' THEN 'Microsoft'
    ELSE ticker
  END as metric,
  close_price as value
FROM stock_prices
WHERE $__timeFilter(date)
ORDER BY time
```

#### 시각화 설정
- **Visualization type:** Time series (기본값)
- **Title:** "Stock Price Trends"
- **Y축 단위:** Currency → USD (선택사항)

#### 스타일 설정
- **Legend:** Bottom 위치
- **Line width:** 2
- **Fill opacity:** 10%

### 2. RSI 지표 게이지 (RSI Indicators)

#### 쿼리 설정
```sql
SELECT 
  CASE 
    WHEN ticker = '005930.KS' THEN 'Samsung'
    WHEN ticker = '000660.KS' THEN 'SK Hynix'
    WHEN ticker = '035420.KS' THEN 'NAVER'
    WHEN ticker = 'AAPL' THEN 'Apple'
    WHEN ticker = 'NVDA' THEN 'NVIDIA'
    WHEN ticker = 'TSLA' THEN 'Tesla'
    WHEN ticker = 'MSFT' THEN 'Microsoft'
    ELSE ticker
  END as metric,
  rsi as value
FROM technical_indicators
WHERE date = (SELECT MAX(date) FROM technical_indicators WHERE ticker = technical_indicators.ticker)
AND rsi IS NOT NULL
ORDER BY ticker
```

#### 게이지 설정
- **Visualization:** Gauge
- **Min value:** 0
- **Max value:** 100
- **Title:** "RSI Indicators"

#### Thresholds (임계값) 설정
| 값 | 색상 | 의미 |
|----|------|------|
| 0-30 | 빨간색 | 과매도 |
| 30-70 | 초록색 | 정상 |
| 70-100 | 빨간색 | 과매수 |

### 3. 포트폴리오 성과 테이블 (Portfolio Performance)

#### 쿼리 설정
```sql
SELECT 
  s.company_name as "Company",
  p.ticker as "Ticker",
  p.quantity as "Shares",
  p.buy_price as "Buy Price ($)",
  sp.close_price as "Current Price ($)",
  (sp.close_price - p.buy_price) * p.quantity as "P&L ($)",
  ROUND(((sp.close_price - p.buy_price) / p.buy_price * 100), 2) as "P&L (%)"
FROM portfolios p
JOIN stocks s ON p.ticker = s.ticker
JOIN (
  SELECT ticker, close_price,
         ROW_NUMBER() OVER (PARTITION BY ticker ORDER BY date DESC) as rn
  FROM stock_prices
) sp ON p.ticker = sp.ticker AND sp.rn = 1
WHERE p.status = 'holding'
ORDER BY p.ticker
```

#### 테이블 설정
- **Visualization:** Table
- **Title:** "Portfolio Performance"

#### 컬럼 스타일링
- **P&L (%)** 컬럼: 
  - Display mode: Color background
  - Thresholds: 음수(빨간색), 0(노란색), 양수(초록색)

### 4. 자산 배분 파이차트 (Portfolio Distribution)

#### 쿼리 설정
```sql
SELECT 
  s.company_name as metric,
  SUM(p.quantity * sp.close_price) as value
FROM portfolios p
JOIN stocks s ON p.ticker = s.ticker
JOIN (
  SELECT ticker, close_price,
         ROW_NUMBER() OVER (PARTITION BY ticker ORDER BY date DESC) as rn
  FROM stock_prices
) sp ON p.ticker = sp.ticker AND sp.rn = 1
WHERE p.status = 'holding'
GROUP BY s.company_name
ORDER BY value DESC
```

#### 파이차트 설정
- **Visualization:** Pie chart
- **Title:** "Portfolio Distribution"
- **Legend:** Right 위치
- **Unit:** Currency → USD

### 5. AI 매매 추천 테이블 (AI Recommendations)

#### 쿼리 설정
```sql
SELECT 
  s.company_name as "Company",
  r.ticker as "Ticker",
  r.recommendation_type as "Recommendation",
  ROUND(r.confidence_score, 1) as "Confidence (%)",
  r.target_price as "Target Price ($)",
  r.reason as "Reason",
  DATE(r.created_at) as "Date"
FROM ai_recommendations r
JOIN stocks s ON r.ticker = s.ticker
WHERE r.valid_until >= CURRENT_DATE OR r.valid_until IS NULL
ORDER BY r.confidence_score DESC
LIMIT 10
```

#### 테이블 설정
- **Title:** "AI Trading Recommendations"
- **Confidence (%)** 컬럼 색상 설정:
  - 0-50: 빨간색 (낮은 신뢰도)
  - 50-75: 노란색 (보통 신뢰도)  
  - 75-100: 초록색 (높은 신뢰도)

---

## ⚙️ 고급 설정 및 커스터마이징

### 대시보드 설정

#### Time Range 설정
- 기본값: Last 30 days
- 권장값: Last 90 days (더 많은 데이터 표시)
- 실시간: Last 5 minutes (개발/테스트용)

#### 자동 새로고침 설정
1. 대시보드 상단 **🔄 Refresh** 버튼 클릭
2. **Auto refresh** 간격 선택:
   - 30s (실시간 모니터링)
   - 1m (일반적 사용)
   - 5m (배터리 절약)

#### 변수 (Variables) 설정

**주식 선택 변수:**
1. **Dashboard settings** (⚙️) → **Variables** → **Add variable**
2. **Name:** `ticker`
3. **Type:** Query
4. **Query:**
```sql
SELECT ticker as __text, ticker as __value 
FROM stocks 
ORDER BY ticker
```
5. **Multi-value:** ✅ (체크)

**사용 예시:**
```sql
SELECT date as time, ticker as metric, close_price as value
FROM stock_prices
WHERE ticker IN ($ticker) AND $__timeFilter(date)
ORDER BY time
```

### 알림 설정

#### RSI 과매도/과매수 알림
1. RSI 패널에서 **Alert** 탭
2. **Create Alert** 클릭
3. **Conditions:**
   - RSI < 30 (과매도)
   - RSI > 70 (과매수)
4. **Notification channels:** Email, Slack 등 설정

#### 급등/급락 알림
```sql
-- 일일 변동률 5% 이상
SELECT 
  ticker,
  (close_price - LAG(close_price, 1) OVER (PARTITION BY ticker ORDER BY date)) / 
  LAG(close_price, 1) OVER (PARTITION BY ticker ORDER BY date) * 100 as daily_change
FROM stock_prices
WHERE date = CURRENT_DATE
AND ABS((close_price - LAG(close_price, 1) OVER (PARTITION BY ticker ORDER BY date)) / 
        LAG(close_price, 1) OVER (PARTITION BY ticker ORDER BY date) * 100) > 5
```

### 테마 및 디자인

#### 다크 테마 적용
1. **왼쪽 하단 프로필** → **Preferences**
2. **UI Theme:** Dark 선택
3. **Save** 클릭

#### 커스텀 색상 팔레트
- **Field options** → **Standard options** → **Color scheme**
- 주식별 고유 색상 지정 가능

---

## 📱 모바일 최적화

### 모바일 레이아웃
1. **Dashboard settings** → **General**
2. **Tags:** mobile 추가
3. **Panel 크기 조정:**
   - 작은 화면용: 12 width
   - 큰 화면용: 6 width

### 반응형 디자인
- **Auto-fit panels:** ✅
- **Minimum width:** 300px
- **Mobile-friendly queries:** 데이터 포인트 제한

---

## 🔧 문제 해결

### 데이터소스 연결 문제

#### "Database Connection Failed" 오류
**원인과 해결:**

| 오류 메시지 | 원인 | 해결 방법 |
|-------------|------|-----------|
| Connection refused | PostgreSQL 미실행 | `docker-compose ps` 확인 후 재시작 |
| Invalid credentials | 잘못된 계정 정보 | admin/admin123 재확인 |
| Unknown host | 잘못된 호스트 | `stock-db:5432` 사용 |
| SSL required | SSL 설정 문제 | SSL Mode를 `disable`로 설정 |

#### 연결 테스트 명령어
```bash
# PostgreSQL 컨테이너 상태 확인
docker-compose ps stock-db

# PostgreSQL 로그 확인
docker-compose logs stock-db

# 직접 연결 테스트
docker-compose exec stock-db psql -U admin -d stocktrading -c "SELECT COUNT(*) FROM stock_prices;"
```

### 빈 차트 문제

#### "No data" 표시되는 경우
1. **Time range 확장:** Last 90 days로 변경
2. **쿼리 검증:** Query Inspector로 결과 확인
3. **데이터 존재 확인:**
```sql
SELECT COUNT(*) FROM stock_prices;
SELECT MIN(date), MAX(date) FROM stock_prices;
```

#### 쿼리 오류 해결
- **SQL 문법 확인:** PostgreSQL 호환 문법 사용
- **테이블명 확인:** 대소문자 구분 주의
- **날짜 필터:** `$__timeFilter(date)` 매크로 사용

### 성능 최적화

#### 느린 쿼리 해결
1. **인덱스 활용:**
```sql
-- 자주 사용하는 쿼리용 인덱스
CREATE INDEX IF NOT EXISTS idx_stock_prices_ticker_date 
ON stock_prices(ticker, date DESC);
```

2. **데이터 제한:**
```sql
-- LIMIT 절 추가
SELECT * FROM stock_prices 
WHERE $__timeFilter(date) 
ORDER BY date DESC 
LIMIT 1000;
```

3. **집계 쿼리 사용:**
```sql
-- 일별 데이터만 표시
SELECT 
  DATE(date) as time,
  ticker as metric,
  AVG(close_price) as value
FROM stock_prices
WHERE $__timeFilter(date)
GROUP BY DATE(date), ticker
ORDER BY time;
```

### 권한 관련 문제

#### "Access denied" 오류
```bash
# 사용자 권한 확인
docker-compose exec stock-db psql -U admin -d stocktrading -c "\du"

# 테이블 권한 확인
docker-compose exec stock-db psql -U admin -d stocktrading -c "\dt"
```

### 메모리 사용량 문제

#### Grafana 메모리 최적화
1. **docker-compose.yml** 수정:
```yaml
grafana:
  environment:
    - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
    - GF_ALERTING_ENABLED=false  # 알림 비활성화
    - GF_METRICS_ENABLED=false   # 메트릭 수집 비활성화
  deploy:
    resources:
      limits:
        memory: 512M
```

2. **Dashboard 최적화:**
   - 패널 수 제한 (8개 이하 권장)
   - 자동 새로고침 간격 늘리기 (5분 이상)
   - 복잡한 쿼리 최소화

---

## 📊 추천 대시보드 템플릿

### 기본 모니터링 대시보드

**구성:**
```
┌─────────────────┬─────────────────┐
│   주가 추이      │   RSI 게이지     │
│   (Time series) │   (Gauge)      │
├─────────────────┴─────────────────┤
│         포트폴리오 성과 테이블        │
│            (Table)               │
├─────────────────┬─────────────────┤
│   자산 배분      │   AI 추천        │
│   (Pie chart)   │   (Table)       │
└─────────────────┴─────────────────┘
```

### 고급 분석 대시보드

**추가 패널:**
- MACD 히스토그램
- 볼린저 밴드 차트
- 거래량 분석
- 상관관계 히트맵

**쿼리 예시:**
```sql
-- MACD 히스토그램
SELECT 
  date as time,
  ticker as metric,
  macd_histogram as value
FROM technical_indicators
WHERE $__timeFilter(date) AND macd_histogram IS NOT NULL
ORDER BY time;

-- 거래량 분석  
SELECT 
  date as time,
  ticker as metric,
  volume as value
FROM stock_prices
WHERE $__timeFilter(date)
ORDER BY time;
```

---

## 📚 추가 리소스

### 유용한 Grafana 플러그인

1. **Clock Panel:** 현재 시간 표시
2. **Worldmap Panel:** 지역별 주식 시장 표시
3. **Table Panel:** 고급 테이블 기능

### 커뮤니티 대시보드

- **Grafana.com** 에서 "Stock Market" 검색
- 템플릿 다운로드 후 데이터소스만 변경

### 공식 문서

- **Grafana 공식 문서:** https://grafana.com/docs/
- **PostgreSQL 데이터소스:** https://grafana.com/docs/grafana/latest/datasources/postgres/
- **Panel 옵션:** https://grafana.com/docs/grafana/latest/panels/

---

## ⚠️ 주의사항

### 보안 권장사항

1. **기본 비밀번호 변경**
   - admin/admin123을 프로덕션에서 사용 금지
   - 강력한 비밀번호로 변경

2. **네트워크 보안**
   - 외부 접근 시 HTTPS 설정
   - 방화벽 규칙 적용

3. **데이터 백업**
   - 대시보드 설정 정기 백업
   - PostgreSQL 데이터 백업

### 성능 고려사항

1. **쿼리 최적화**
   - 불필요한 데이터 조회 최소화
   - 인덱스 활용

2. **새로고침 간격**
   - 실시간이 필요하지 않으면 5분 이상 설정
   - 배터리 및 네트워크 절약

3. **데이터 보존 정책**
   - 오래된 데이터 아카이브
   - 디스크 공간 관리

---

**문서 버전**: v1.0.0  
**최종 업데이트**: 2025-08-29  
**작성자**: Claude Code Assistant

**지원 문의**: 시스템 관련 문의는 프로젝트 GitHub Issues를 이용해주세요.